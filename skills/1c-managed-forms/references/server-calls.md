# Серверные вызовы: бюджет по случаям

Норма расхода и правила для формы — в теле навыка. Здесь остальные случаи
стандарта 487 «Минимизация количества серверных вызовов и трафика», область
применения — «управляемое приложение, мобильное приложение, обычное
приложение».

Материал этого файла существует ради **чисел и точных имён**: сколько вызовов
допустимо на какое действие, через что передавать данные, какие свойства
платформы вызывают сервер незаметно.

## Что считается вызовом (пункт 1.1)

«Общее количество серверных вызовов складывается из обращений на сервер,
которые выполняет платформа 1С:Предприятие, и вызовов, которые выполняются
из клиентского кода конфигурации — **отклонение, привносимое конфигурацией
по отношению к платформе**».

Считается именно отклонение: то, что платформа делает сама, в бюджет
разработчика не входит.

**Единственное исключение, названное стандартом:** «неявный серверный вызов
при первом обращении к клиентскому общему модулю. Например, вызов
`ТорговыеПредложенияКлиент.ОбновитьПредложения` приведёт к вызову сервера,
если общий модуль `ТорговыеПредложенияКлиент` ещё не был скомпилирован».

**Медленное соединение и мобильный клиент** (пункт 1.2): там «следует
оптимизировать не только количество вызовов, но и **объём передаваемых
данных**». Отладку «рекомендуется проводить в **режиме имитации задержек
серверных вызовов**».

## Запуск клиентского приложения (пункт 2.1)

«В простейшем случае, код конфигурации, выполняемый при запуске клиентского
приложения, **не должен приводить к обращениям на сервер**. Речь идёт о коде
обработчиков `ПередНачаломРаботыСистемы`, `ПриНачалеРаботыСистемы` модуля
приложения».

Если данные с сервера всё же нужны, стандарт даёт два требования:

- «**не следует** напрямую вызывать серверные процедуры и функции из кода
  модуля приложения, модуля управляемого приложения и модуля внешнего
  соединения»;
- «правильно: **за один вызов** на сервер передавать на клиент все параметры,
  необходимые для запуска приложения».

Если параметры нужны из разных мест клиентского кода — «следует разместить
такую функцию в общем серверном модуле **с повторным использованием
возвращаемых значений**. При первом вызове этой функции происходит одно
обращение к серверу, после чего полученное значение автоматически кешируется
платформой на клиенте для всех повторных вызовов».

```bsl
// Общий серверный модуль ПараметрыПовтИсп, повторное использование значений
Функция ПараметрыРаботыКлиента() Экспорт
	Параметры = Новый Структура();
	Параметры.Вставить("ИнформационнаяБазаФайловая", ОбщегоНазначения.ИнформационнаяБазаФайловая());
	Возврат Параметры;
КонецФункции
```

## Периодические вызовы (пункт 3.1)

Случаи, которые стандарт относит сюда: «передача данных о замерах
производительности…, проверка динамического изменения конфигурации или
изменения прав пользователя с предложением перезапустить программу, проверка
завершения работы пользователей, проверка актуальности курсов валют…, проверка
напоминаний пользователя».

**Число:** «следует объединять вызовы в один и делать его период **по
возможности не чаще одного раза в двадцать минут**».

**И приём взамен:** «рекомендуется использовать механизм уведомлений клиентов
1С:Предприятия (см. `МенеджерУведомленийКлиента`) для отправки оповещений
клиентским сеансам **с сервера** вместо периодических вызовов с клиента».

**Формы начальной страницы — особый случай** (пункт 3.2.2): «Для форм
на начальной странице (то есть открытых условно постоянно) **не рекомендуется**
делать периодические серверные вызовы, а вместо этого рекомендуется сделать
кнопку `Обновить`. Если же периодические вызовы необходимы, то делать их
**отключёнными по умолчанию**, а их период делать **не чаще одного раза
в минуту**».

Для проверки состояния сервера в отдельно открытой форме «допустимо делать
бесконтекстные (быстрые) серверные вызовы, однако следует выбирать **как можно
больший период** вызова в рамках решаемой задачи».

## Открытие формы (пункт 4.1)

«следует открывать форму **за один вызов** с помощью метода глобального
контекста `ОткрытьФорму`… Для передачи параметров в форму следует использовать
параметр этих методов `Параметры`».

Пример неправильного из стандарта — два вызова вместо одного:

```bsl
// неправильно: сначала за настройкой, потом за формой
НастройкаПроксиСервера = СерверныйМодуль.НастройкаПроксиСервера();
ОткрытьФорму("ОбщаяФорма.ПараметрыПроксиСервера", Новый Структура("Настройка", НастройкаПроксиСервера));

// правильно: настройку берёт сама форма в ПриСозданииНаСервере
ОткрытьФорму("ОбщаяФорма.ПараметрыПроксиСервера");
```

## Выбор из справочника и глобальная команда

**Выбор из справочника** (пункт 5.2): «допускается выполнять **только один**
серверный вызов из кода, к которому приводит вызов метода глобального контекста
`ОткрытьФорму`». И (пункт 6.3): «если после выбора… необходимо выполнить
бизнес-логику, которую возможно отработать только на сервере, то **допустимо**
выполнять её за один дополнительный серверный вызов».

**Глобальная команда** (пункт 6.1): «допускается выполнять **только один**
серверный вызов из кода. В случае если команда открывает форму, то этот вызов
должен выполняться при вызове метода глобального контекста `ОткрытьФорму`».

## Отчёт (пункт 7.1)

«не допускается выполнять дополнительных серверных вызовов из кода
конфигурации. В частности, **при открытии формы отчёта не допускается**
выполнять обращений к серверу… в обработчиках клиентских событий формы, таких
как `ПриОткрытии` и `ПриПовторномОткрытии`».

Что вместо — названо прямо: «открывать форму отчёта с параметром
**`СформироватьПриОткрытии = Истина`**, либо установить параметр формы
`СформироватьПриОткрытии` в значение `Истина` в обработчике
`ПриСозданииНаСервере`».

## Подбор элементов (пункты 8.1–8.2)

**Почему нельзя параметром** (8.1): «**не рекомендуется** передавать
потенциально большой массив данных в качестве параметра формы подбора.
Потенциально большой массив данных, хранимый в форме подбора в параметре типа
`ДанныеФормыКоллекция`, **может оказаться на клиенте не полным** за счёт
оптимизации работы управляемой формы. Как результат — для передачи параметра
будет выполнено дополнительное "дочитывание" данных формы с сервера».

**Схема через временное хранилище** (8.2), «чтение и запись которого **должна
выполняться на сервере**». Бюджет назван по обеим сторонам:

- «Открытие формы подбора из клиентского кода **должно приводить не более чем
  к двум обращениям на сервер**» — положить список во временное хранилище
  (первый вызов) и открыть форму, передав адрес (второй);
- «Закрытие формы подбора **должно приводить не более чем к двум обращениям
  на сервер**» — форма подбора кладёт выбранное во временное хранилище (первый),
  форма документа забирает оттуда (второй).

```bsl
// в форме документа
&НаКлиенте
Процедура ОбработчикКомандыПодбора()
	АдресТоваровВХранилище = ПоместитьТоварыВХранилище();
	ПараметрыПодбора = Новый Структура();
	ПараметрыПодбора.Вставить("АдресТоваровДокумента", АдресТоваровВХранилище);
	ОткрытьФорму("Документ.РасходТовара.Форма.ФормаПодбора", ПараметрыПодбора, Элементы.Товары);
КонецПроцедуры

&НаСервере
Функция ПоместитьТоварыВХранилище()
	Возврат ПоместитьВоВременноеХранилище(
		Объект.Товары.Выгрузить(, "Товар,Цена,Количество"), УникальныйИдентификатор);
КонецФункции
```

Форма подбора забирает список в `ПриСозданииНаСервере`, а при закрытии кладёт
результат **по тому же адресу**, который получила параметром, — чтобы форма
документа знала, откуда читать.

## Обработчики записи (пункт 9)

«Вместо дополнительного вызова сервера в клиентских обработчиках `ПередЗаписью`
и `ПослеЗаписи` модуля формы **следует** размещать серверную логику
в аналогичных серверных обработчиках `ПередЗаписьюНаСервере`,
`ПриЗаписиНаСервере`, `ПослеЗаписиНаСервере`, и передавать данные на клиент
через параметр обработчиков `ПараметрыЗаписи` или реквизиты формы».

Смысл параметра показан примером: вызывать `УправлениеФормой`
в `ПослеЗаписиНаСервере` «с передачей и проверкой параметра о последующем
закрытии формы: если форма будет закрыта, управлять ей не имеет смысла».

Разбор того же правила со стороны границы клиент-сервер — в `client-server.md`.

## Неявные серверные вызовы (пункт 10)

Полного перечня стандарт не даёт — «полный список таких методов и свойств
приведён в синтаксис-помощнике». Названы:

- методы `ПолучитьИзВременногоХранилища`, `УдалитьИзВременногоХранилища`;
- **свойства поля формы** `Заголовок`, `Доступность`, `Подсказка`,
  `ТолькоПросмотр` «и т.п.»;
- `ПолучитьФункциональнуюОпциюИнтерфейса`, `ПолучитьФункциональнуюОпциюФормы`.

То есть присваивание `Элементы.Поле.Доступность = Ложь` в клиентском коде —
это серверный вызов, которого в тексте не видно.

«**Следует избегать многократного** вызова таких методов и свойств… Вместо
этого их **необходимо объединять в один серверный вызов**: использовать одно
временное хранилище или переносить логику в серверную процедуру, которая
однократно вызывается с клиента».

## Объём передаваемых данных (пункты 11–12)

**Не больше нужного** (пункт 11): «**Не следует** передавать с клиента
на сервер больше данных, чем нужно для выполнения задачи. Например, если для
выполнения процедуры на сервере требуется только одно значение реквизита,
то **не следует делать вызов контекстным**».

**Параметры — со `Знач`** (пункт 12): «**Не рекомендуется** возвращать значения
параметров обратно с сервера на клиент, а вместо этого рекомендуется делать
функцию, которая вернёт требуемые данные. Все параметры в процедурах
и функциях, которые вызываются с клиента на сервер **рекомендуется указывать
с префиксом `Знач`** (тогда значение не будет возвращаться с клиента на сервер
целиком, включая содержимое сложных типов — `Структура`, `Массив` и другие)».

Где именно это касается — перечислено там же: «экспортные серверные процедуры,
размещённые в общих модулях с признаком `Вызов сервера`, процедуры в модулях
форм и команд с директивами компиляции `&НаСервере`, `&НаСервереБезКонтекста`
или **без директив** (неявное определение `&НаСервере`)».

## Сколько кода оставлять на клиенте (стандарт 629)

Область применения — «управляемое приложение, мобильное приложение, обычное
приложение».

**Общее требование** (пункт 1.1): «**Необходимо минимизировать** объём кода,
который выполняется на стороне клиентского приложения. В частности **не следует**
выполнять на клиенте сложные алгоритмы, требующие значительных ресурсов
компьютера. В таких случаях выполнение алгоритма на клиенте может занимать
гораздо больше времени, чем передача управления с клиента на сервер, выполнение
алгоритма на сервере и возврат результата обратно на клиент».

Причин две, и вторая часто решает: «как правило, клиентский компьютер менее
производительный, чем серверный» и «клиентский код выполняется интерпретатором
встроенного языка, который **в веб-клиенте работает заметно медленнее**, чем
в тонком или толстом клиенте».

**Встречное правило** (пункт 1.2), без которого первое загоняет всё на сервер:
«**Рекомендуется оставлять на клиенте** такие алгоритмы, скорость работы
которых заведомо быстрее, чем затраты, необходимые на вызов **одной** серверной
функции. Например, перерасчёт доступности элементов управления в форме при
изменении пользователем данных выполняется на клиенте, т.к. контекстный
серверный вызов для сложной формы может сделать работу пользователя в этой
форме неприемлемой».

**Исключение** (пункт 2.1): «когда функциональная подсистема предназначена для
работы с программным обеспечением, установленным на клиентском компьютере.
Например, работа с торговым оборудованием, интеграция с клиент-банком,
формирование печатных форм в офисные программы».

**Режимные ветки — препроцессором** (пункт 2.2):

```bsl
#Если ВебКлиент Тогда
	Предупреждение(НСтр("ru = 'Загрузка адресного классификатора не доступна в веб-клиенте.'"));
#Иначе
	ОткрытьФорму("РегистрСведений.АдресныйКлассификатор.Форма.ФормаЗагрузкиАдресногоКлассификатора");
#КонецЕсли
```

Это не противоречит запрету стандарта 439 на `#Если Сервер` и `#Если Клиент`
в клиент-серверных общих модулях: там речь о **определении контекста
исполнения**, здесь — о режиме клиента, который препроцессор знает достоверно.

## Значения, влияющие на поведение приложения (стандарт 459)

«В том случае, если поведение целого ряда форм или команд зависит от одних
и тех же значений… для минимизации клиент-серверного взаимодействия
**рекомендуется применять общие модули с повторным использованием возвращаемых
значений** (свойство "Повторное использование возвращаемых значений" равно
**"На время сеанса"**)».

**Условие применимости названо**: «допускается, если изменение соответствующего
значения в течение сеанса **не является критичным**, т.е. в течение всего сеанса
может использоваться однажды полученное значение».

**И требование к самой функции**: «функции такого общего модуля **должны
за один вызов возвращать сразу все значения**, которые могут понадобиться в тех
или иных обстоятельствах». Пример источника — три параметра занятости
(«Время занятости по умолчанию», «Время начала рабочего дня», «Время окончания
рабочего дня») возвращаются одной структурой, а не тремя вызовами.

## Чего в этих стандартах нет

- **цены одного вызова в числах** — ни в миллисекундах, ни в байтах
  передаваемого контекста;
- **способа увидеть, что именно уехало на сервер** вместе с контекстным
  вызовом: что уезжают изменённые данные формы, сказано, а посмотреть их
  состав нечем;
- **директив компиляции как темы** — их разбор у соседа
  `1c-code-conventions`, здесь только выбор между контекстным
  и внеконтекстным вызовом;
- **поведения веб-клиента отдельно от прочих** — ключи диагностики описаны
  для клиентского приложения, и это оговорка соседа `1c-quality-gates`.
