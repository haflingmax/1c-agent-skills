# Где живёт код объекта

Здесь лежит то, что ищут по точному имени: признаки и постфиксы четырёх видов
общих модулей, перечень уточняющих постфиксов, порядок шагов внутри
`ОбработкаЗаполнения`, правило встречных проверок и сигнатуры обработчиков
представления.

Область применения берётся первой строкой документа. У стандартов 469, 396
и 463 она одинакова — «управляемое приложение, мобильное приложение, обычное
приложение». У стандарта 746 она **уже**: «управляемое приложение, мобильное
приложение», обычного приложения в ней нет. Строк вида «Область применения
(уточнение): …», сужающих охват для отдельных пунктов, ни в одном из четырёх
документов не встречается — искал по всему тексту каждого.

**Оговорка о нумерации.** Везде, где ниже стоит «пункт N», номер есть в самом
стандарте. Собственной нумерации в этом файле нет.

## Четыре контекста общего модуля (стандарт 469)

Зачем модуль заводят (пункт 1.1): «Общие модули создаются для реализации
процедур и функций, объединенных по некоторому признаку. Как правило, в один
общий модуль помещаются процедуры и функции одной подсистемы конфигурации
(продажи, закупки) или процедуры и функции сходного функционального назначения
(работа со строками, общего назначения)».

Выбор контекста (пункт 1.2): «При разработке общих модулей **следует выбирать
один из четырех контекстов выполнения кода**».

| № | Тип общего модуля | Пример наименования | Вызов сервера | Сервер | Внешнее соединение | Клиент (обычное приложение) | Клиент (управляемое приложение) |
|---|---|---|---|---|---|---|---|
| 1 | Серверный | ОбщегоНазначения (или ОбщегоНазначенияСервер) | | + | + | + | |
| 2 | Серверный для вызова с клиента | ОбщегоНазначенияВызовСервера | + | + | | | |
| 3 | Клиентский | ОбщегоНазначенияКлиент (или ОбщегоНазначенияГлобальный) | | | | + | + |
| 4 | Клиент-серверный | ОбщегоНазначенияКлиентСервер | | + | + | + | + |

**Чем проверена таблица.** Выгрузка ИТС разворачивает её в один столбец: сперва
семь заголовков подряд, затем по строкам — номер, название, пример имени
и голые плюсы без привязки к колонкам. Соответствие восстановлено не по
позиции, а по тексту пунктов 2.1–2.4, где набор признаков каждого вида модуля
назван словами, и сверено по числу плюсов в выгрузке: строка 1 — три плюса
и три признака в пункте 2.1; строка 2 — два плюса, а пункт 2.2 называет один
признак «Сервер (флажок Вызов сервера установлен)», то есть отмеченными
оказываются две колонки — «Вызов сервера» и «Сервер»; строка 3 — два плюса
и два признака в пункте 2.3; строка 4 — четыре плюса и четыре признака
в пункте 2.4. Расхождений нет.

**Чего по выгрузке таблицы не видно вовсе.** Пустые ячейки в неё не попадают:
после имени строки идут только плюсы, без привязки к колонкам. Поэтому какая
именно колонка пуста, из таблицы не следует — то, что у строк 1 и 4 не отмечен
«Вызов сервера», выведено **из текста пунктов** 2.1 и 2.4, из скобок «флажок
Вызов сервера снят» и «флажок Вызов сервера сброшен». Восстановление держится
на тексте, а не на позициях в выгрузке.

### Серверный (пункт 2.1)

«Серверные общие модули предназначены для размещения серверных процедур
и функций, **не доступных для использования из клиентского кода**. В них
реализуется вся внутренняя серверная бизнес-логика приложения. Для корректной
работы конфигурации в режимах внешнего соединения, управляемого и обычного
приложений, серверные процедуры и функции **следует размещать** в общих модулях
с признаками:

> Сервер (флажок Вызов сервера снят),
> Клиент (обычное приложение) ,
> Внешнее соединение .»

Признаков три, и порядок в источнике такой: «Сервер (флажок Вызов сервера
снят)», «Клиент (обычное приложение)», «Внешнее соединение». Своей оценки
о том, какой из них пропускают чаще, источник не даёт, и здесь её нет.

Что этим достигается, сказано прямо: «В таком случае гарантируется возможность
вызова серверных процедур и функций с параметрами мутабельных типов (например,
`СправочникОбъект` , `ДокументОбъект` и т.п.). Как правило, это: обработчики
подписок на события документов, справочников и т.п., которые принимают
в качестве параметра мутабельное значение (объект). серверные процедуры
и функции, в которые в качестве параметра передается объект из модулей
справочников, документов и пр., а также из модулей с подписками на события».

Имя: «называются по общим правилам именования объектов метаданных. Например:
`РаботаСФайлами` , `FilesOperations` ». Постфикс здесь **не обязателен**,
а условен: «В отдельных случаях **для предотвращения конфликта имен
со свойствами глобального контекста** может быть добавлен постфикс "Сервер"
(англ. "Server" ). Например: `РегламентныеЗаданияСервер` ,
`ScheduledJobsServer` ».

### Серверный для вызова с клиента (пункт 2.2)

«Серверные общие модули для вызова с клиента содержат серверные процедуры
и функции, доступные для использования из клиентского кода. Они составляют
клиентский программный интерфейс сервера приложения. Такие процедуры и функции
размещаются в общих модулях с признаком:

> Сервер (флажок Вызов сервера установлен)»

Признак назван **один**, в единственном числе.

Имя — здесь модальность жёсткая: «называются по общим правилам именования
объектов метаданных и **должны именоваться с постфиксом "ВызовСервера"**
(англ. "ServerCall" ). Например: `РаботаСФайламиСлужебныйВызовСервера` ,
`FilesOperationsInternalServerCall` ».

И ограничение на параметры: «Следует иметь в виду, что экспортные процедуры
и функции в таких общих модулях **не должны содержать параметров мутабельных
типов** ( `СправочникОбъект` , `ДокументОбъект` и т.п.), так как их передача
из (или в) клиентского кода невозможна».

### Клиентский (пункт 2.3)

«Клиентские общие модули содержат клиентскую бизнес-логику (функциональность,
определенную только для клиента) и имеют признаки:

> Клиент (управляемое приложение) ,
> Клиент (обычное приложение) .»

**Исключение оттуда же:** «Исключение составляют случаи, когда клиентские
процедуры и функции должны быть доступны только в режиме управляемого
приложения (только в режиме обычного приложения или только в режиме внешнего
соединения). В таких случаях, **допустима иная комбинация двух этих
признаков**».

Имя: «именуются с постфиксом "Клиент" (англ. "Client" ). Например:
`РаботаСФайламиКлиент` , `FilesOperationsClient` ».

### Клиент-серверный (пункт 2.4)

Модальность мягкая, и цель названа: «**Для того чтобы избежать дублирования
кода, рекомендуется** создавать клиент-серверные общие модули с теми процедурами
и функциями, содержание которых одинаково на сервере и на клиенте. Такие
процедуры и функции размещаются в общих модулях с признаками:

> Клиент (управляемое приложение) ,
> Сервер (флажок Вызов сервера сброшен),
> Клиент (обычное приложение) ,
> Внешнее соединение .»

Признаков четыре — больше, чем у любого другого вида.

**И встречное ограничение, без которого пункт читается наполовину:** «В то же
время, как только возникает необходимость ветвить код в клиент-серверных общих
модулях на серверный и клиентский, то **не следует использовать для этого
инструкции препроцессора**. Вместо этого, функциональность, различную для
клиента и для сервера, **рекомендуется** реализовывать по общим правилам
в модулях соответствующего типа – см. пп. 2.1 и 2.3».

Обоснование дано источником целиком, вместе с ценой: «Такое явное разделение
клиентской и серверной бизнес-логики продиктовано соображениями повышения
модульности прикладного решения, упрощения контроля со стороны разработчика над
клиент-серверным взаимодействием и снижением риска ошибок из-за принципиальных
отличий требований к разработке клиентского и серверного кода (необходимость
минимизации кода, выполняемого на клиенте, разной доступностью объектов и типов
платформы и др.). **При этом нужно иметь в виду неизбежное увеличение числа
общих модулей в конфигурации**».

Имя: «Общие модули этого вида именуются с постфиксом "КлиентСервер" (англ.
"ClientServer" ). Например: `РаботаСФайламиКлиентСервер` ,
`FilesOperationsClientServer` ».

И оговорка там же — про модули, которые под правило четырёх контекстов
не попадают: «Особым случаем смешанных клиент-серверных модулей являются модули
форм и команд, которые специально предназначены для реализации серверной
и клиентской бизнес-логики в одном модуле».

## Имена общих модулей (стандарт 469, пункты 3.1–3.2)

**Основное правило** (пункт 3.1): «Имена общих модулей **рекомендуется** строить
по общим правилам именования объектов метаданных. Название общего модуля
**должно совпадать** с названием подсистемы или отдельного механизма, процедуры
и функции которой он реализует. **Рекомендуется избегать** в названиях общих
модулей таких общих слов как "Процедуры", "Функции", "Обработчики", "Модуль",
"Функциональность" и т.п. и применять их только в исключительных случаях, когда
они более полно раскрывают назначение модуля».

Три разные модальности в одном пункте: строить по общим правилам —
рекомендуется, совпадение с названием подсистемы — должно, общих слов —
рекомендуется избегать (а не запрещено: оговорка про исключительные случаи
стоит рядом).

Там же — зачем нужны постфиксы: «Для того чтобы различать общие модули одной
подсистемы, которые созданы для реализации процедур и функций, выполняемых
в разных контекстах, **рекомендуется** задавать им постфиксы, описанные ранее
в пп. 2.1-2.4».

**Уточняющие постфиксы** (пункт 3.2): «Дополнительно к общим модулям **могут
быть добавлены** уточняющие постфиксы». Подпунктов пять — 3.2.1–3.2.5,
пересчитано по нумерации документа:

| Пункт | Постфикс | Англ. | Когда | Пример из стандарта |
|---|---|---|---|---|
| 3.2.1 | Глобальный | Global | «Для глобальных модулей» | РаботаСФайламиСлужебныйГлобальный, FilesOperationsInternalGlobal |
| 3.2.2 | ПолныеПрава | FullAccess | «Модули, выполняющиеся в привилегированном режиме, имеющие признак Привилегированный» | РаботаСФайламиСлужебныйПолныеПрава, FilesOperationsInternalFullAccess |
| 3.2.3 | ПовтИсп / КлиентПовтИсп | Cached / ClientCached | «Модули, предназначенные для реализации на сервере или на клиенте функций с повторным использованием возвращаемых значений (на время вызова или на время сеанса)» | РаботаСФайламиСлужебныйКлиентПовтИсп, FilesOperationsInternalClientCached |
| 3.2.4 | Переопределяемый / КлиентПереопределяемый | Overridable / ClientOverridable | «Серверные и клиентские модули библиотечных конфигураций… с процедурами и функциями, допускающие изменение своей реализации» | РаботаСФайламиКлиентПереопределяемый, FilesOperationsClientOverridable |
| 3.2.5 | Локализация / КлиентЛокализация | Localization / Client Localization | «В локализуемых конфигурациях… модули, реализующие национальную специфику» | ЭлектроннаяПодписьСлужебныйЛокализация, ElectonicSignatureInternalLocalization |

Таблица собрана из подпунктов, идущих в выгрузке отдельными абзацами (в самом
стандарте таблицы здесь нет), поэтому сокращения в колонке «Когда» помечены
многоточием, а полные формулировки — в тексте пунктов.

Три подробности, которые легко потерять:

- **3.2.1 отменяет другой постфикс:** «Для глобальных модулей добавляется
  постфикс "Глобальный" (англ. "Global" ), **в этом случае постфикс "Клиент"
  добавлять не следует**»;
- **3.2.4 поясняет, о каких конфигурациях речь:** библиотечные — это те,
  «которые предназначены не для самостоятельного использования, а для
  разработки других конфигураций»;
- **3.2.5 уточняет, какие конфигурации локализуемые:** те, «на базе которых
  выпускаются национальные прикладные решения для различных стран или
  регионов».

В пункте 3.2.5 английский вариант напечатан как «"Client Localization "» —
с пробелом перед закрывающей кавычкой; орфография источника сохранена,
но постфикс в имени пишется слитно, что видно по примеру. Там же в примере
латиницей — `ElectonicSignatureInternalLocalization`, с опечаткой источника
в слове Electonic.

## Структура обработчика `ОбработкаЗаполнения` (стандарт 396, пункт 3)

**Пометка «Методическая рекомендация (полезный совет)» стоит в середине
документа — после пункта 2 и непосредственно перед пунктом 3.** На пункты 1
и 2 она не распространяется: там модальность обычная («следует выполнять»,
«следует использовать исключения»). Пометка относится только к структуре шагов
ниже.

Само правило (пункт 3): «**Рекомендуется** придерживаться следующей логической
структуры обработчика `ОбработкаЗаполнения` (**отдельные шаги могут быть
пропущены**)».

Скобка про пропуск шагов — часть правила, а не оговорка составителя: структура
не является обязательной последовательностью.

**Шаг первый** (пункт 3.1): «Выполнение специального заполнения в зависимости
от типа параметра `ДанныеЗаполнения` ». Пример оттуда же (переносы строк
восстановлены — выгрузка ИТС склеивает код в одну строку):

```bsl
ТипДанныхЗаполнения = ТипЗнч(ДанныеЗаполнения);
Если ТипДанныхЗаполнения = Тип("Структура") Тогда
	ЗаполнитьДокументПоОтбору(ДанныеЗаполнения);
ИначеЕсли ТипДанныхЗаполнения = Тип("ДокументСсылка.ЗаказКлиента") Тогда
	ЗаполнитьДокументНаОснованииЗаказаКлиента(ДанныеЗаполнения);
// ...
```

**Шаг второй** (пункт 3.2): «Выполнение общего заполнения, с целью заполнить
значениями по умолчанию реквизиты, которые не были заполнены специальным
заполнением. **При этом необходимо предварительно проверять реквизит
на заполненность**».

```bsl
Если Не ЗначениеЗаполнено(Подразделение) Тогда
	Подразделение = ЗначениеНастроекПовтИсп.ПодразделениеПоУмолчанию();
КонецЕсли;
```

Там же — предпочтение свойству метаданных перед кодом: «Также при заполнении
реквизитов значениями по умолчанию **следует, по возможности**, использовать
свойство метаданных **"Значение заполнения"** . Значение, указанное в этом
свойстве будет автоматически присваиваться реквизиту при выходе из обработчика
`ОбработкаЗаполнения` , **в случае если параметр `СтандартнаяОбработка`
установлен в `Истина` , и реквизит не был заполнен в обработчике**».

**Условия здесь два, и они соединены союзом «и»:** `СтандартнаяОбработка`
должна остаться `Истина` **и** реквизит не должен быть заполнен в обработчике.
Выполнение одного из двух не даёт автоматического присваивания.

Обоснование источник даёт своё, и оно про качество кода, а не про
работоспособность: «Выполнение данной рекомендации позволит уменьшить
количество логических ошибок заполнения и повысит читаемость кода».

## Проверки в транзакции записи и вне её (стандарт 463)

Раздел озаглавлен в источнике так: «Проверки, выполняемые в и вне транзакции
записи объекта».

Исходная точка (пункт 2.1): «Проверки в обработчике
`ОбработкаПроверкиЗаполнения` выполняются **вне транзакции записи объекта**.
Поскольку в случае некорректного заполнения объекта выполнение операции будет
прервано еще до записи объекта в базу данных, то размещение проверок в этом
обработчике является наиболее эффективным». И встречное предупреждение оттуда
же, вместе с условием, при котором оно действует: «**При выполнении
внетранзакционных проверок в обработчике `ОбработкаПроверкиЗаполнения`**
необходимо учитывать тот факт, что **новое состояние объекта ещё
не записано**. Если требуется выполнить запрос к тем или иным данным системы,
например, прочитать признак `ВидНоменклатуры` для товаров, выбранных
в табличной части документа, "отталкиваясь" от данных документа, то такую
поверку можно выполнить, применяя **сохранение необходимых для запроса данных
во временные таблицы**».

### Куда переносят проверки целостности (начало пункта 2.2)

Пункт 2.2 открывается запретом, ради которого и написан весь этот блок:

> «В то же время, в обработчике `ОбработкаПроверкиЗаполнения` **не следует
> размещать проверки, которые должны гарантировать целостное состояние объекта
> или зависящих от него данных** (например, движений) на которые рассчитывает
> система. Поэтому для реквизитов, некорректные значения которых могут привести
> к рассогласованности данных в информационной базе, проверку корректности
> **следует выполнять в обработчиках событий, возникающих в транзакции записи —
> `ПередЗаписью`, `ПриЗаписи`, `ОбработкаПроведения`** (для документов).»

Без этой половины пункт читается наоборот: пункт 2.1 хвалит
`ОбработкаПроверкиЗаполнения` как наиболее эффективный способ, а всё дальнейшее
транзакционные проверки отговаривает — и получается, что проверять целостность
негде.

### Два случая транзакционных проверок (продолжение пункта 2.2)

«Для транзакционных проверок, в свою очередь, выделяются **два случая**:

> Проверка состояния движений, формируемых документами оперативного учета.
> Такие проверки довольно часто встречаются в приложениях с оперативным учетом.
>
> Проверка состояния других объектов информационной базы, ссылки на которых
> содержатся в текущем объекте. **Такие проверки следует применять очень
> редко.** Не следует злоупотреблять количеством проверок в транзакции записи
> объекта. Следует помнить, что внутри транзакции записи имеет смысл выполнять
> только проверки таких ресурсов или таких правил соответствия объектов друг
> другу, **которые не изменяются без проверок всеми участниками процесса**.»

Число «два» здесь — слово источника, не подсчёт.

### Первый случай: остатки ресурса

«В первом случае, проверку остатков некоторого ресурса **имеет смысл выполнять
в транзакции записи только в том случае, если все документы выполняют такую же
проверку в транзакции записи**. Если хоть один из документов, изменяющих
ресурс, делает это без проверок, выполнение проверок другими участниками
процесса бессмысленно и такие проверки **необходимо выполнять вне транзакции**.
**Исключением может быть только случай, когда документ, который выполняет
изменение контролируемого ресурса без проверок, вводится крайне редко.**
Например, не смотря на то, что документ "Инвентаризация товаров" изменяет
остатки товаров без проверок, эта ситуация допустима в виду того, что он
вводится крайне редко. **Каждое такое исключение из правила должно быть
оправданным.**»

Обрыв цитаты перед словом «Исключением» превращает правило в запрет, которого
в стандарте нет. Орфография источника сохранена: «не смотря на то»,
«в виду того».

### Второй случай: встречная проверка

«Во втором случае, если при записи Подразделения в транзакции записи
выполняется проверка, что сотрудник, выбранный в качестве руководителя
подразделения, имеет должность "Руководитель", то при записи Сотрудника **также
должна выполняться и "встречная" проверка этого же правила**: нельзя записать
Сотрудника с должностью отличной от "Руководитель", если он указан
руководителем того или иного подразделения. Поскольку правило, что "Сотрудник",
выбранный руководителем подразделения, должен иметь должность "Руководитель",
может быть нарушено как при записи подразделения, так и при записи сотрудника,
то и проверка **должна выполняться или в транзакции записи обоих объектов, или
вне транзакции записи обоих объектов (а может и не выполняться вообще)**».

Вариантов в концовке три, и третий — в скобках: оба объекта в транзакции, оба
вне транзакции, либо проверки нет вовсе. Запрещена только несимметричность.

## Обмен данными и отложенное проведение (стандарт 463, пункт 1.4)

**Перед этим пунктом — и только перед ним — в документе стоит пометка
«Методическая рекомендация (полезный совет)».** Она напечатана после пункта 1.3
и до пункта 1.4; на пункты 1.1–1.3 и на весь раздел о транзакционных проверках
(пункты 2.1–2.2) не распространяется.

«В случае использования в конфигурации подсистемы "Обмен данными" Библиотеки
стандартных подсистем обработчик `ОбработкаПроверкиЗаполнения` **вызывается при
проведении документов, после их загрузки из сообщения обмена**. Для отключения
некоторых проверок в этом режиме в обработчике **можно анализировать**
дополнительное свойство объекта `ДополнительныеСвойства.ОтложенноеПроведение`».

Модальность мягкая — «можно анализировать»; такая же («можно выполнить») стоит
и в пункте 2.1. Имя
свойства приведено целиком, вместе с составляющей `ДополнительныеСвойства`:
искать его следует именно так.

## Переопределение представления объекта (стандарт 746, пункт 1)

Область применения этого стандарта — «управляемое приложение, мобильное
приложение»; обычного приложения в ней нет, в отличие от остальных трёх
документов этого файла.

Где живут обработчики и что они умеют: «С помощью данных обработчиков **модуля
менеджера объекта** можно переопределить представление объекта информационной
базы, которое выводится **в полях форм и в списках**. Пример реализации:»

Сигнатуры и пример из источника (переносы строк восстановлены — в выгрузке ИТС
код склеен в одну строку; текст, имена и порядок параметров не менялись):

```bsl
Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
	Поля.Добавить("Наименование");
	Поля.Добавить("Дата");
	СтандартнаяОбработка = Ложь;
КонецПроцедуры

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	Наименование = ?(ПустаяСтрока(Данные.Наименование), НСтр("ru = 'Без описания'"), Данные.Наименование);
	Дата = Формат(Данные.Дата, ?(ПолучитьФункциональнуюОпцию("ИспользоватьДатуИВремяВСрокахЗадач"), "ДЛФ=DT", "ДЛФ=D"));
	Представление = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(НСтр("ru = '%1 от %2'"), Наименование, Дата);
	СтандартнаяОбработка = Ложь;
КонецПроцедуры
```

Параметры по именам: у `ОбработкаПолученияПолейПредставления` — `Поля`
и `СтандартнаяОбработка`; у `ОбработкаПолученияПредставления` — `Данные`,
`Представление` и `СтандартнаяОбработка`. В обоих обработчиках примера
`СтандартнаяОбработка` устанавливается в `Ложь`. Поля, перечисленные
в `ОбработкаПолученияПолейПредставления`, — те самые, к которым обращается
`ОбработкаПолученияПредставления` через параметр `Данные`: в примере это
«Наименование» и «Дата». Объяснения этой связи стандарт не даёт, но и подмены
здесь нет — имена в примере совпадают буквально.
