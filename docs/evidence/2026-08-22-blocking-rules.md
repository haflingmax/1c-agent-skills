# Доказательство блокирующих правил запуском (2026-08-22)

Платформа: `C:/Program Files/1cv8/8.3.27.2325/bin/1cv8.exe`.
База: `D:/1C/base/trade` (файловая, одноразовая).
Скрипт: `tools/prove-blocking-rules.ps1`, запуск `1cv8.exe DESIGNER` через
`Start-Process -Wait` (без ожидания процесс — оконное приложение — возвращает
управление мгновенно, ничего не создав; это выглядело бы как тихий отказ).

## Результат прогона

| Имя        | КодВыхода | DumpResult | ФайлСоздан | Журнал                                    | Примечание |
|------------|-----------|------------|------------|--------------------------------------------|------------|
| канон      | 0         | 0          | True       | Сохранение конфигурации успешно завершено | — |
| строчными  | 0         | 0          | True       | Сохранение конфигурации успешно завершено | — |
| выдуманный | 0         | 0          | False      | Сохранение конфигурации успешно завершено | файл по указанному пути не создан; в рабочем каталоге появился посторонний файл `ToFile`, 4973 байта — тот же размер, что у настоящей выгрузки этой базы (`real.cf` из независимой проверки ниже) |

(`канон` — ключ `/DumpCfg`; `строчными` — ключ `/dumpcfg`; `выдуманный` — ключ
`/DumpCfgToFile`, не задокументированный приложением 7.)

### Куда делась работа при выдуманном ключе

Источник: ручная проверка координатора задачи 5 (2026-08-22) в чистом каталоге —

```
1cv8 DESIGNER /F D:\1C\base\trade /DumpCfgToFile <путь>\target.cf /DisableStartupDialogs /Out ... /DumpResult ...

код: 0, DumpResult: 0
target.cf создан: False
в рабочем каталоге появился: ToFile, 4973 байта
```

Независимо повторено при подготовке этого отчёта, в изолированном рабочем каталоге
(`Start-Process -WorkingDirectory`, без общих файлов с чужими прогонами): обычный
`/DumpCfg` в тот же каталог создаёт файл `real.cf` размером 4973 байта — то же
число, что и у `ToFile`. Совпадение размера с независимым запуском исключает
случайность.

Вывод: платформа разбирает `/DumpCfgToFile` как `/DumpCfg` с хвостом `ToFile`,
принятым за позиционный аргумент — имя файла. Конфигурация выгружается по-настоящему,
только не туда: в файл `ToFile` в текущем рабочем каталоге, а путь, переданный
командой, отбрасывается как лишний. Это не «команда ничего не сделала» — это
«команда сделала не туда».

Сырой `proof.json` (создаётся скриптом в `%TEMP%\1c-proof\proof.json`):

```json
[
  {
    "Имя": "канон",
    "КодВыхода": 0,
    "DumpResult": "0",
    "ФайлСоздан": true,
    "Журнал": "Сохранение конфигурации успешно завершено"
  },
  {
    "Имя": "строчными",
    "КодВыхода": 0,
    "DumpResult": "0",
    "ФайлСоздан": true,
    "Журнал": "Сохранение конфигурации успешно завершено"
  },
  {
    "Имя": "выдуманный",
    "КодВыхода": 0,
    "DumpResult": "0",
    "ФайлСоздан": false,
    "Журнал": "Сохранение конфигурации успешно завершено"
  }
]
```

## Вывод

- **Блокировка `/DumpCfgToFile` обоснована.** Платформа принимает ключ без ошибки
  (`КодВыхода 0`, `DumpResult 0`), пишет в журнал «Сохранение конфигурации успешно
  завершено» — и при этом файл `.cf` не создаётся (`ФайлСоздан False`). И
  `DumpResult`, и `/Out`-журнал в этом случае врут: успех определяется только по
  факту появления файла на диске, как и требует глобальное ограничение брифа.
  Правило проверяльщика, блокирующее `/DumpCfgToFile` как противоречащее
  документированному составу приложения 7, подтверждено запуском. Куда фактически
  делась работа — см. «Куда делась работа при выдуманном ключе» выше: файл не
  пропал, он лёг не туда.

- **Блокировка по регистру ключа не обоснована.** И канонический `/DumpCfg`, и
  строчный `/dumpcfg` одинаково создают файл конфигурации (`ФайлСоздан True` в
  обоих случаях). Платформа 8.3.27.2325 нечувствительна к регистру этого ключа —
  значит, правило, которое блокировало бы вариант в нижнем регистре, блокировало
  бы работающий вызов, и его накладывать нельзя.
