# План реализации: доработка ядра по находкам ревью

> **Исполнителю:** ОБЯЗАТЕЛЬНЫЙ ПОДНАВЫК — `superpowers:subagent-driven-development`
> либо `superpowers:executing-plans`. Шаги помечены чекбоксами `- [ ]`.

**Цель:** закрыть 14 выстоявших находок ревью и 30 мелких так, чтобы чинилась причина,
а не следствие, и чтобы ни одна из 43 сильных сторон не пропала по дороге.

**Подход:** пять задач по классам дефекта, а не четырнадцать по находкам. Каждая находка
воспроизводится **до** правки; регресс из сильных сторон заводится в первой задаче
и проходит после каждой следующей.

**Спека:** [docs/specs/2026-08-23-core-remediation-design.md](../specs/2026-08-23-core-remediation-design.md)

**Находки:** [docs/reviews/2026-08-23-core-review.md](../reviews/2026-08-23-core-review.md) —
там точные формулировки Н-01…Н-14 и М-01…М-30. **Это источник требований по каждой правке:**
план задаёт порядок и способ, документ находок — что именно исправить.

**Общий план:** [docs/plan.md](../plan.md). План подчинён ему, относится к этапу ЯДРО-6.

## Global Constraints

- **Находка закрывается только парой «до» и «после».** Сначала воспроизведение —
  команда, прогон, вывод. Не воспроизвелась — не чинить, а записать, почему.
- **Мелкие М-хх подтверждаются перед правкой каждая отдельно.** Они не проходили
  состязательной проверки, часть наверняка ложные.
- **Регресс проходит целиком после каждой задачи.** Заводится в задаче 1.
- Проверки: `python tools/check-skills.py` → 0, `python tools/check-sources.py` → 0,
  `python -m pytest tests/ -q` → все зелёные.
- Кириллица: `PYTHONIOENCODING=utf-8` при запуске Python.
- Платформа для прогонов: `C:/Program Files/1cv8/8.3.27.2325/bin/1cv8.exe`,
  база `D:/1C/base/trade` — файловая, одноразовая, портить разрешено.
- `1cv8.exe` запускать только через `Start-Process -Wait`: он оконное приложение
  и без ожидания возвращает управление немедленно, ничего не создав.
- Успех операции 1С определяется по результату, а не по коду возврата и не по `/Out`.
- Скрипт блокирует только противоречащее документированному составу приложения 7;
  зависящее от замысла — предупреждение с кодом 0. **Новая блокировка законного — Critical.**
- Выгрузка ИТС в `_its/` под gitignore. Инструменты, читающие её, обязаны работать
  и без неё — либо честно отказываться с понятным сообщением.
- Прогоны Kilo и Claude Code — только там, где задача их прямо требует: они минутами.

---

### Задача 1: Проверяльщик узнаёт чужие контракты, и заводится регресс

Класс I. Закрывает Н-04 (Critical), Н-01; мелкие М-01, М-04, М-05, М-08, М-09,
М-15, М-22, М-23, М-24, М-27.

**Файлы:**
- Создать: `tools/check-manifests.py`
- Создать: `tests/test_check_manifests.py`
- Создать: `tests/test_strengths.py`
- Изменить: `tools/check-skills.py`
- Изменить: `tools/check-sources.py`
- Изменить: `.codex-plugin/plugin.json`
- Изменить: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`
- Изменить: `skills/developing-1c-configurations/SKILL.md` (только строка `description`)

**Интерфейсы:**
- Даёт дальше: `tests/test_strengths.py` — регресс сильных сторон, вызывается
  во всех последующих задачах через `python -m pytest tests/ -q`.
- Даёт дальше: `tools/check-manifests.py` возвращает 0, если манифесты законны.

- [ ] **Шаг 1: Воспроизвести Н-04 — сломанный YAML**

```
cd d:/github.com/haflingmax/1c-agent-skills
PYTHONIOENCODING=utf-8 python -c "import io,re,yaml;t=io.open('skills/developing-1c-configurations/SKILL.md',encoding='utf-8').read();yaml.safe_load(re.match(r'^---\s*\n(.*?)\n---',t,re.S).group(1))"
```

Ожидается: падение с `mapping values are not allowed here`. Причина — двоеточие
с пробелом внутри `description` на позиции 216. Если не падает — записать и не чинить.

- [ ] **Шаг 2: Воспроизвести Н-04 — манифест Codex**

Найти официальный валидатор: `find "C:/Users/ashil/.claude/plugins/cache" -name "validate_plugin.py"`.
Если найден — прогнать по репозиторию и записать вывод. Если не найден — проверить
поле `hooks` по документации Codex: спека описывает его как строку-путь, а в манифесте
лежит `{}`, и каталога `hooks/` не существует. Записать, чем именно подтверждено.

- [ ] **Шаг 3: Написать падающие тесты манифестов и фронтматтера**

Создать `tests/test_check_manifests.py`:

```python
"""Проверка манифестов плагина и разбора фронтматтера настоящим YAML.

Соответствует находкам Н-04 и Н-01 из docs/reviews/2026-08-23-core-review.md.
Запуск: python -m pytest tests/test_check_manifests.py -v
"""
import json
import re
from pathlib import Path

import yaml

ROOT = Path(__file__).resolve().parent.parent


def frontmatter(path):
    text = path.read_text(encoding="utf-8")
    m = re.match(r"^---\s*\n(.*?)\n---\s*\n", text, re.S)
    assert m, "нет фронтматтера в %s" % path
    return m.group(1)


def test_frontmatter_is_valid_yaml():
    """Н-04: фронтматтер обязан разбираться настоящим YAML, не регуляркой."""
    for skill_md in sorted((ROOT / "skills").rglob("SKILL.md")):
        data = yaml.safe_load(frontmatter(skill_md))
        assert isinstance(data, dict), skill_md
        assert "name" in data and "description" in data, skill_md


def test_codex_manifest_has_no_rejected_fields():
    """Н-04: поле hooks официальный валидатор Codex отвергает."""
    m = json.loads((ROOT / ".codex-plugin" / "plugin.json").read_text(encoding="utf-8"))
    assert "hooks" not in m, "поле hooks отвергается валидатором Codex"


def test_plugin_descriptions_agree():
    """М-09: два манифеста не должны расходиться в описании."""
    a = json.loads((ROOT / ".claude-plugin" / "plugin.json").read_text(encoding="utf-8"))
    b = json.loads((ROOT / ".codex-plugin" / "plugin.json").read_text(encoding="utf-8"))
    assert a["description"] == b["description"], "описания манифестов разошлись"


def test_version_declared_once():
    """М-05: версия не должна дублироваться в манифесте и в записи маркета."""
    mk = json.loads((ROOT / ".claude-plugin" / "marketplace.json").read_text(encoding="utf-8"))
    entry = mk["plugins"][0]
    assert "version" not in entry, (
        "версия объявлена и в plugin.json, и в записи маркета; "
        "при расхождении молча побеждает plugin.json")
```

- [ ] **Шаг 4: Убедиться, что тесты падают**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/test_check_manifests.py -v
```

Ожидается: падают `test_frontmatter_is_valid_yaml`, `test_codex_manifest_has_no_rejected_fields`,
и, если описания разошлись, `test_plugin_descriptions_agree`.

- [ ] **Шаг 5: Починить фронтматтер**

В `skills/developing-1c-configurations/SKILL.md` заменить двоеточие с пробелом
в описании на тире. Длину держать не больше 340 знаков — каталог навыков в Codex
урезается по бюджету. Тело навыка не трогать.

- [ ] **Шаг 6: Починить манифесты**

Убрать `hooks` из `.codex-plugin/plugin.json`. Привести описания обоих манифестов
к одному тексту. Убрать `version` из записи маркета, оставив в `plugin.json`.

- [ ] **Шаг 7: Написать проверяльщик манифестов**

Создать `tools/check-manifests.py` — по образцу `tools/check-skills.py`: без внешних
зависимостей кроме `yaml`, сообщения по-русски, вывод через `print`, код 1 при нарушении.
Проверяет: фронтматтер разбирается YAML; в манифесте Codex нет отвергаемых полей;
описания манифестов совпадают; версия объявлена один раз; версия соответствует semver.

- [ ] **Шаг 8: Расширить check-skills.py по мелким находкам**

Каждую подтвердить, затем починить. Точные формулировки — в документе находок:

- **М-22, Н-01:** `SPEC_FIELDS` дополнить полем `compatibility`; добавить проверку
  его длины (1–500 знаков).
- **М-01:** проверка глубины ссылок считает слэши, а документация говорит о цепочке
  ссылок. Привести к смыслу документации либо, если однозначно нельзя, — понизить
  до предупреждения и объяснить в комментарии.
- **М-04, М-15, М-23:** порог `MAX_BODY_CHARS = 20000` выведен из английской оценки,
  а тела кириллические. Пересчитать под кириллицу и подписать, откуда взято число.
- **М-24:** линтер отвергает формулировки, которые образец рекомендует прямым текстом.
  Проверить на конкретном примере из находки; если подтвердится — ослабить правило.

- [ ] **Шаг 9: Починить М-27 в check-sources.py**

Прослеживаемость проверяется наличием слова, а не разрешимостью ссылки: абзац
со словом «руководство» проходит, даже если руководства такого нет. Подтвердить
на примере, затем усилить проверку.

- [ ] **Шаг 10: Завести регресс сильных сторон**

Создать `tests/test_strengths.py`. Из 43 сильных сторон (раздел «Сильные стороны»
документа находок) отобрать те, что проверяются машиной, и выразить тестами.
Начать минимум с этих, они сформулированы измеримо:

```python
"""Регресс сильных сторон набора.

Каждый тест соответствует свойству, отмеченному ревью как ценное. Смысл —
не дать доработке сломать то, что уже сделано верно. Источник формулировок:
docs/reviews/2026-08-23-core-review.md, раздел «Сильные стороны».
"""
import json
import re
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
SKILLS = sorted((ROOT / "skills").rglob("SKILL.md"))


def test_names_within_limits():
    """Имена: только строчная латиница, цифры и дефис, не длиннее 64 знаков."""
    for md in SKILLS:
        name = re.search(r"^name:\s*(.+)$", md.read_text(encoding="utf-8"), re.M).group(1).strip()
        assert re.fullmatch(r"[a-z0-9-]+", name), name
        assert len(name) <= 64, name
        assert "claude" not in name and "anthropic" not in name, name


def test_references_are_one_level():
    """Справочные файлы не ссылаются дальше: цепочек SKILL.md → файл → файл нет."""
    for ref in sorted((ROOT / "skills").rglob("references/*.md")):
        links = re.findall(r"\]\(([^)]+\.md)\)", ref.read_text(encoding="utf-8"))
        assert not links, "%s ссылается дальше: %s" % (ref, links)


def test_plugin_layout():
    """.claude-plugin несёт только манифесты, skills лежит в корне."""
    assert (ROOT / "skills").is_dir()
    inside = {p.name for p in (ROOT / ".claude-plugin").iterdir()}
    assert inside <= {"plugin.json", "marketplace.json"}, inside


def test_marketplace_source_is_repo_root():
    """source: './' — маркет и плагин это один репозиторий."""
    mk = json.loads((ROOT / ".claude-plugin" / "marketplace.json").read_text(encoding="utf-8"))
    assert mk["plugins"][0]["source"] == "./"


def test_no_hard_dependency_on_other_plugins():
    """superpowers объявлен рекомендацией, а не зависимостью."""
    m = json.loads((ROOT / ".claude-plugin" / "plugin.json").read_text(encoding="utf-8"))
    assert "dependencies" not in m, (
        "жёсткая зависимость выключит весь набор при отсутствии плагина")
```

Остальные сильные стороны, не выразимые машиной, свести в
`docs/reviews/2026-08-23-strengths-manual.md` — список для человека при финальном ревью.
Разделение обязательно и делается явно.

- [ ] **Шаг 11: Прогнать всё**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/ -q
PYTHONIOENCODING=utf-8 python tools/check-skills.py
PYTHONIOENCODING=utf-8 python tools/check-sources.py
PYTHONIOENCODING=utf-8 python tools/check-manifests.py
```

Все четыре — код 0.

- [ ] **Шаг 12: Зафиксировать**

```
git add tools/ tests/ .codex-plugin/ .claude-plugin/ skills/ docs/reviews/
git commit -m "Проверяльщик узнаёт контракт Codex, фронтматтер разбирается YAML

Набор не проходил официальный валидатор Codex: манифест содержал поле hooks,
которое валидатор отвергает. Фронтматтер ядра не являлся валидным YAML —
двоеточие с пробелом в описании заставляло yaml.safe_load падать. Свой
проверяльщик обоих дефектов не видел: он знал только требования к SKILL.md
и разбирал фронтматтер регуляркой.

Заведён второй проверяльщик манифестов и регресс сильных сторон — свойств,
отмеченных ревью как ценные, чтобы доработка их не сломала."
```

---

### Задача 2: Справочник ключей пересобирается целиком

Класс II. Закрывает Н-06 (Critical), Н-07, Н-08; мелкую М-20.

**Файлы:**
- Изменить: `tools/derive-glued.py` → переписать в `tools/build-cli-keys.py`
- Изменить: `skills/1c-build-and-db/scripts/cli-keys.json`
- Изменить: `tests/test_check_1c_cli.py`
- Изменить: `tools/prove-blocking-rules.ps1` (перепрогон)

**Интерфейсы:**
- Потребляет: выгрузку `_its/cmdline/its-pril7-full.json` (под gitignore).
- Даёт дальше: `cli-keys.json` с полем `modes` — **списком**, а не строкой;
  полными списками `opts`; ключом `/@`. Задача 3 работает на этих данных.

- [ ] **Шаг 1: Воспроизвести Н-06**

```
cd skills/1c-build-and-db/scripts
PYTHONIOENCODING=utf-8 python check-1c-cli.py "1cv8 CREATEINFOBASE File=\"d:/base\" /DumpResult d:/rc.txt"
```

Ожидается код 1 с жалобой на режим: каталог хранит у `/DumpResult` один режим,
а приложение 7 описывает его в двух — 7.2.3 и 7.4.17.

- [ ] **Шаг 2: Воспроизвести Н-07 и Н-08**

```
PYTHONIOENCODING=utf-8 python -c "import json;d=json.load(open('cli-keys.json',encoding='utf-8'))['ключи'];print('/@ есть:', '/@' in d);print('opts у /CheckConfig:', len(d['/CheckConfig'].get('opts',[])))"
```

Ожидается: `/@` отсутствует; число опций у `/CheckConfig` равно 12 — след отсечки
после ASCII-сортировки. Сверить с синопсисом из приложения 7 и записать разницу.

- [ ] **Шаг 3: Написать проверку полноты как падающий тест**

Добавить в `tests/test_check_1c_cli.py`:

```python
def test_catalog_completeness_against_source():
    """Н-07, Н-08: состав каталога сверяется с приложением 7 программно.

    Источник — _its/cmdline/its-pril7-full.json. Если выгрузки нет на машине,
    тест пропускается: каталог _its/ под gitignore и у пользователя его нет.
    """
    import pytest
    src = ROOT / "_its" / "cmdline" / "its-pril7-full.json"
    if not src.exists():
        pytest.skip("выгрузка ИТС недоступна: каталог _its/ под gitignore")
    keys = CATALOG["ключи"]
    assert "/@" in keys, "документированный ключ /@ (раздел 7.3.11) отсутствует"
    assert isinstance(keys["/DumpResult"]["modes"], list), "режим обязан быть списком"
    assert len(keys["/DumpResult"]["modes"]) >= 2, (
        "/DumpResult описан в двух режимах: 7.2.3 и 7.4.17")
```

- [ ] **Шаг 4: Убедиться, что тест падает**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/test_check_1c_cli.py::test_catalog_completeness_against_source -v
```

- [ ] **Шаг 5: Переписать сборщик каталога**

`tools/derive-glued.py` заменить на `tools/build-cli-keys.py`, который собирает
каталог целиком, а не проставляет один признак. Требования:

- поле `modes` — **список** режимов; ключ, встреченный в нескольких разделах,
  получает все;
- `opts` — полный список из синопсиса ключа, **без отсечки по числу**;
- признак `glued` сохраняется;
- каталог `_its/` под gitignore: при его отсутствии скрипт **не падает молча**,
  а сообщает, что источник недоступен, и завершается с кодом 2 (М-20);
- в конце печатает сверку: сколько ключей, сколько с несколькими режимами,
  сколько опций всего.

- [ ] **Шаг 6: Пересобрать каталог и сверить**

```
PYTHONIOENCODING=utf-8 python tools/build-cli-keys.py
```

Записать в отчёт: было ключей/опций, стало ключей/опций. Рост опций ожидается
не менее чем на 30 — столько ревью насчитало потерянными.

- [ ] **Шаг 7: Привести проверяльщик к новому полю modes**

В `skills/1c-build-and-db/scripts/check-1c-cli.py` проверка режима читает `mode`
как строку. Перевести на список: ключ законен, если запрошенный режим есть среди
его режимов. Ключ без режимов — «общий».

- [ ] **Шаг 8: Перепрогнать доказательства целиком**

```
powershell -NoProfile -File tools/prove-blocking-rules.ps1
```

Пересборка меняет данные под всеми доказательствами. Сверить таблицу с
`docs/evidence/2026-08-22-blocking-rules.md` и обновить файл, если поведение изменилось.
**Расхождение — находка, а не повод править таблицу под результат.**

- [ ] **Шаг 9: Прогнать всё**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/ -q
PYTHONIOENCODING=utf-8 python tools/check-skills.py
PYTHONIOENCODING=utf-8 python tools/check-sources.py
PYTHONIOENCODING=utf-8 python tools/check-manifests.py
```

- [ ] **Шаг 10: Зафиксировать**

```
git add tools/build-cli-keys.py skills/1c-build-and-db/scripts/cli-keys.json tests/ docs/evidence/
git rm tools/derive-glued.py
git commit -m "Справочник ключей пересобран целиком, а не залатан в трёх местах

Каталог хранил у ключа один режим, тогда как приложение 7 описывает
/DumpResult в двух, и проверяльщик блокировал законную команду. Отсутствовал
документированный ключ /@. Списки опций были обрезаны после сортировки,
и потерялось три десятка документированных опций.

Три находки оказались следствиями одной причины — приблизительного разбора
приложения 7, поэтому каталог пересобран целиком, а полнота сверяется тестом.
Доказательства блокирующих правил перепрогнаны на живой платформе."
```

---

### Задача 3: Модель платформы в разборе имени ключа

Класс III. Закрывает Н-05 (Critical); мелкие М-14, М-16, М-25, М-28.

**Файлы:**
- Изменить: `skills/1c-build-and-db/scripts/check-1c-cli.py`
- Изменить: `tests/test_check_1c_cli.py`
- Изменить: `docs/evidence/2026-08-22-blocking-rules.md`

**Интерфейсы:**
- Потребляет: `cli-keys.json` с полем `modes`-списком из задачи 2.

- [ ] **Шаг 1: Воспроизвести Н-05 на живой платформе**

```powershell
$exe = "C:\Program Files\1cv8\8.3.27.2325\bin\1cv8.exe"
$w = "$env:TEMP\n05"; Remove-Item $w -Recurse -Force -EA 0; New-Item -ItemType Directory -Force $w | Out-Null
$a = @('DESIGNER','/F','D:\1C\base\trade','/LoadCfgFromFile',"$w\t.cf",'/DisableStartupDialogs','/Out',"$w\l.log",'/DumpResult',"$w\rc.txt")
$p = Start-Process -FilePath $exe -ArgumentList $a -Wait -PassThru -NoNewWindow -WorkingDirectory $w
"код: $($p.ExitCode)"; Get-ChildItem $w | Select-Object Name, Length
```

Ожидается: платформа разбирает `/LoadCfgFromFile` как `/LoadCfg` с позиционным
аргументом `FromFile` — то есть берёт **самое длинное совпадение среди всех ключей**,
а не среди двенадцати слитных. Сравнить с тем, что говорит наш `known_key`.

- [ ] **Шаг 2: Написать падающий тест на совпадение с платформой**

```python
def test_known_key_matches_platform_longest_match():
    """Н-05: платформа берёт самое длинное совпадение среди ВСЕХ ключей.

    Проверено запуском: /LoadCfgFromFile платформа разбирает как /LoadCfg
    с позиционным аргументом FromFile, а не как /L со значением.
    """
    keys = CATALOG["ключи"]
    assert mod.known_key("/LoadCfgFromFile", keys) == "/LoadCfg"
    assert mod.known_key("/DumpCfgToFile", keys) == "/DumpCfg"
    assert mod.known_key("/Lru", keys) == "/L"
```

- [ ] **Шаг 3: Убедиться, что тест падает**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/test_check_1c_cli.py::test_known_key_matches_platform_longest_match -v
```

- [ ] **Шаг 4: Привести известный ключ к модели платформы**

`known_key` обязан брать самое длинное совпадение среди **всех** ключей каталога,
а не среди двенадцати слитных. Признак `glued` остаётся — он отличает случай,
когда хвост это значение (`/Lru`), от случая, когда хвост это позиционный аргумент
(`/LoadCfgFromFile`). Оба разрешаются, но второй обязан давать предупреждение:
точного такого ключа нет.

- [ ] **Шаг 5: Мелкие находки этой области**

Каждую подтвердить, затем починить. Формулировки — в документе находок.

- **М-14:** в `MODE_WORDS` есть режим `CONFIG`, которого нет ни в каталоге,
  ни в тексте ошибки. Выяснить, существует ли он, и либо описать, либо убрать.
- **М-16:** тест проверяет не то, что заявляет: docstring ссылается на один запуск,
  а проверка делает другое. Привести в соответствие.
- **М-25:** скрипт не отвечает на `--help`. Добавить.
- **М-28:** множество `DESTRUCTIVE` заведено, но третьего кода возврата
  «изменения есть» нет. Оценить, нужен ли; решение записать с обоснованием.

- [ ] **Шаг 6: Обновить доказательства**

Добавить в `docs/evidence/2026-08-22-blocking-rules.md` результат шага 1: как
платформа разбирает `/LoadCfgFromFile`. Это второе наблюдение того же механизма
после `/DumpCfgToFile`, и теперь оно применено в коде, а не только записано.

- [ ] **Шаг 7: Прогнать всё**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/ -q
PYTHONIOENCODING=utf-8 python tools/check-skills.py
PYTHONIOENCODING=utf-8 python tools/check-sources.py
PYTHONIOENCODING=utf-8 python tools/check-manifests.py
```

- [ ] **Шаг 8: Зафиксировать**

```
git add skills/1c-build-and-db/scripts/check-1c-cli.py tests/ docs/evidence/
git commit -m "Разбор имени ключа приведён к поведению платформы

Наш known_key ограничивал совпадение по префиксу двенадцатью слитными
ключами, тогда как платформа берёт самое длинное совпадение среди всех
и отправляет хвост в позиционный аргумент. Это наблюдалось дважды —
на /DumpCfgToFile и на /LoadCfgFromFile — и было записано в доказательства,
но в модель кода не внесено.

Теперь внесено и проверено запуском."
```

---

### Задача 4: Текст приводится в соответствие с содержимым

Класс IV. Закрывает Н-09 (Critical), Н-10, Н-11, Н-03, Н-02; мелкие М-02, М-03,
М-06, М-07, М-10, М-12, М-19, М-21, М-26, М-29, М-30.

**Файлы:**
- Изменить: `skills/1c-build-and-db/SKILL.md`
- Изменить: `skills/1c-build-and-db/references/load-configuration.md`
- Изменить: `skills/developing-1c-configurations/SKILL.md`
- Изменить: `skills/developing-1c-configurations/references/architecture.md`
- Изменить: `README.md`

- [ ] **Шаг 1: Воспроизвести Н-09**

Прочитать `references/load-configuration.md:39-43` и `SKILL.md:84`. Находка говорит:
ограничение сужено до клиент-серверного варианта там, где источник его не сужает.
Сверить с выгрузкой руководства администратора в `_its/`. Если источник действительно
не сужает — исправить; если сужает — записать, что находка не подтвердилась.

- [ ] **Шаг 2: Воспроизвести Н-11**

```
grep -n "epf\|erf" skills/1c-build-and-db/SKILL.md
```

Описание обещает сборку `epf` и `erf`, маршрутная строка ядра — тоже. Проверить,
есть ли в теле навыка проза про это. Нет — либо написать, либо убрать обещание.

- [ ] **Шаг 3: Воспроизвести Н-10 и Н-03**

Сверить README с фактическим составом набора и с таблицей путей установки.
Н-10: состояние в README отстало. Н-03: ячейка «В проекте» для Codex содержит
прочерк, тогда как Codex ищет навыки в `.agents/skills` от текущего каталога вверх.

- [ ] **Шаг 4: Исправить крупные находки**

По каждой — точная формулировка в документе находок. Правило: **обещание убирается
или закрывается прозой; третьего не дано.** Формулировки писать в стиле набора:
контрастные пары вместо таблиц «значение → вердикт», признак вместо перечня.

- [ ] **Шаг 5: Исправить мелкие**

Каждую подтвердить перед правкой. Отдельно отметить М-03 и М-21 — они про то,
что текст обещает состояние, а не знание:

- **М-03:** четырнадцать строк таблицы несут «(навыка ещё нет)». Это состояние,
  и каждый выпуск поднавыка делает часть тела неверной. Придумать форму, которая
  не требует правки при каждом выпуске.
- **М-21:** ядро объявляет прогон проверяльщика обязательным условием завершения,
  а собственная приёмка зафиксировала, что в одной из сред это невыполнимо.

- [ ] **Шаг 6: Добавить оглавления (Н-02, М-10)**

В `architecture.md` и `load-configuration.md` — блок `## Содержание` в начале.
Оба длиннее ста строк, документация рекомендует оглавление.

- [ ] **Шаг 7: Прогнать всё и прогнать оба антипода**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/ -q
PYTHONIOENCODING=utf-8 python tools/check-skills.py
PYTHONIOENCODING=utf-8 python tools/check-sources.py
PYTHONIOENCODING=utf-8 python tools/check-manifests.py
powershell -NoProfile -File tools/install-skills.ps1
powershell -NoProfile -File tools/run-prompt.ps1 -Env kilo   -Prompt "Выгрузи конфигурацию базы D:/1C/base/trade в XML."
powershell -NoProfile -File tools/run-prompt.ps1 -Env claude -Prompt "Выгрузи конфигурацию базы D:/1C/base/trade в XML."
```

Правка текстов может сломать поведение. Условие: агент выдал работу и назвал
умолчания вслух; вопросительные знаки не считать — счётчик ловит тернарный
оператор 1С и объявления XML.

- [ ] **Шаг 8: Зафиксировать**

```
git add skills/ README.md
git commit -m "Текст приведён в соответствие с содержимым

Ограничение было сужено до клиент-серверного варианта там, где источник
его не сужает. Описание обещало сборку epf и erf, которой в теле навыка нет.
README отстал от состава набора, а таблица путей установки для Codex
содержала неверную ячейку.

Причина у всех одна: нет проверки соответствия обещаний содержимому.
Найденные расхождения устранены, обещания либо закрыты прозой, либо сняты."
```

---

### Задача 5: Измерение и выпуск

Класс V. Закрывает Н-12, Н-13, Н-14; мелкие М-11, М-13, М-17, М-18.

**Файлы:**
- Изменить: `tools/measure-trigger.ps1`
- Изменить: `skills/1c-build-and-db/scripts/check-1c-cli.py`
- Изменить: `.claude-plugin/plugin.json`
- Изменить: `docs/plan.md`
- Создать: `docs/releasing.md`

- [ ] **Шаг 1: Воспроизвести Н-12**

Прочитать массив `$prompts` в `tools/measure-trigger.ps1`. Все формулировки —
из предметной области набора. Значит замер меряет только полноту: любое расширение
описания оценивается не хуже нейтрального и **не может быть наказано**.

Проверка: добавить в набор две формулировки, где навык подниматься **не должен** —
например про другой язык программирования. Если после этого расширенное описание
даёт ложные срабатывания, замер это покажет.

- [ ] **Шаг 2: Дать замеру отрицательный член**

Добавить в `$prompts` формулировки-отрицания и считать по ним ложные срабатывания.
Итог замера — две величины: сколько поднял там, где надо, и сколько там, где не надо.

- [ ] **Шаг 3: Починить прибор (М-17, М-18)**

Детектор Д-11 ищет жёстко зашитое имя MCP-сервера, которого в прогоне не было.
Для Kilo «Навык» — подстрочный поиск, дающий ложные срабатывания. Привести оба
к структурному признаку либо честно назвать верхней оценкой в отчёте.

- [ ] **Шаг 4: Дать диагностикам идентификаторы (Н-14)**

Сообщения проверяльщика печатаются как `[ошибка]`/`[внимание]` плюс русское
предложение, без идентификатора: на них нельзя сослаться и их нельзя отключить.
Добавить короткий код каждому правилу — по образцу линтеров: `[ошибка K001] …`.

- [ ] **Шаг 5: Двинуть версию и записать правило (Н-13)**

Поднять версию в `.claude-plugin/plugin.json`. Создать `docs/releasing.md` — коротко:
когда двигать версию и почему это обязательно. Основание находки: установившие
через маркет не получат ни одного обновления, пока строка не изменится.

- [ ] **Шаг 6: Обновить общий план**

В `docs/plan.md` пометить закрытыми находки Н-хх и М-хх, подтверждённые прогонами.
Непройденное оставить открытым с указанием, что именно не сошлось. **Не помечать
закрытым то, что не воспроизводилось.**

- [ ] **Шаг 7: Прогнать всё**

```
PYTHONIOENCODING=utf-8 python -m pytest tests/ -q
PYTHONIOENCODING=utf-8 python tools/check-skills.py
PYTHONIOENCODING=utf-8 python tools/check-sources.py
PYTHONIOENCODING=utf-8 python tools/check-manifests.py
```

- [ ] **Шаг 8: Зафиксировать**

```
git add tools/ skills/ .claude-plugin/ docs/
git commit -m "Замер умеет наказывать, диагностики адресуемы, версия двинута

Замер срабатывания меряло только полноту: все формулировки были из предметной
области набора, поэтому любое расширение описания оценивалось не хуже
нейтрального и не могло быть наказано. Добавлены формулировки, где навык
подниматься не должен.

Диагностики проверяльщика получили идентификаторы: на них теперь можно
сослаться. Версия плагина двинута — установившие через маркет не получали
обновлений, пока строка версии не менялась."
```

---

## Самопроверка плана

**Покрытие спеки.** Класс I — задача 1. Класс II — задача 2. Класс III — задача 3.
Класс IV — задача 4. Класс V — задача 5. Механизм «воспроизвести до починки» —
первый шаг каждой задачи и Global Constraints. Регресс сильных сторон — задача 1,
шаг 10, прогоняется в каждой задаче. Перепрогон доказательств после пересборки
справочника — задача 2, шаг 8.

**Покрытие находок.** Н-01, Н-04 — задача 1. Н-06, Н-07, Н-08 — задача 2.
Н-05 — задача 3. Н-02, Н-03, Н-09, Н-10, Н-11 — задача 4. Н-12, Н-13, Н-14 — задача 5.
Все тридцать М-хх распределены: 10 в задачу 1, 1 в задачу 2, 4 в задачу 3,
11 в задачу 4, 4 в задачу 5.

**Порядок.** Задача 2 раньше задачи 3: данные вперёд алгоритма. Задача 1 первая:
там заводится регресс, которым живут остальные.

**Согласованность имён.** `tools/check-manifests.py` создаётся в задаче 1
и вызывается в задачах 2–5. `tests/test_strengths.py` создаётся в задаче 1
и входит в `pytest tests/` везде. Поле `modes` вводится в задаче 2 и потребляется
в задаче 3. `known_key(name, keys)` меняется в задаче 3, сигнатура прежняя.

**Чего план намеренно не делает.** Не закрывает Д-11 (Codex с minimax-m3)
и Д-24 (Kilo и режим совместимости) — они вне области по спеке. Не проектирует
полный порядок выпуска: только правило и одно движение версии.
