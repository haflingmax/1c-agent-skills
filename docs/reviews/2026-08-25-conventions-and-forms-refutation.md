# Опровержение: раздел соглашений и четыре справочника форм

**Дата:** 25.08.2026. Девять проверяющих, каждый сверял свой файл
с первоисточником ИТС (база v8std) через `tools/its-search.py`.
Отчёт собран сводным агентом из их находок, без перепроверки.

Разбор и правки — в коммитах того же дня.

---

**9 critical, 38 important, 37 minor — 84 находки по 5 файлам.**

---

## `d:/github.com/haflingmax/1c-agent-skills/skills/1c-code-conventions/SKILL.md`

### critical

**`SKILL.md:41`** — заголовок «`ТекущаяДата` не используется нигде» и критерий проверки на строке 179 отменяют пункт с исключениями, который в файл не попал вовсе; вместе с разрешением потеряна и обязанность обосновать вызов комментарием. Источник: «Исключения из правил 2 и 3 возможны в отдельных, обоснованных случаях, когда требуется использовать действительно текущее время серверного компьютера. Такие исключения должны быть обоснованы в тексте комментария к вызову» (стандарт 643, пункт 4).

**`SKILL.md:59-63`** — пример оборван на первом операторе, вторая ветка потеряна; `МестноеВремя` и `ЧасовойПоясСеанса` не встречаются нигде в наборе. Источник: «Например: ДатаАктуальностиУниверсальная = УниверсальноеВремя(ПолнотекстовыйПоиск.ДатаАктуальности()); ДатаАктуальности = МестноеВремя(ДатаАктуальностиУниверсальная, ЧасовойПоясСеанса());» (стандарт 643, пункт 2.3).

**`SKILL.md:125-126`** — запрет «не присваивать Ложь» перенесён на `СтандартнаяОбработка` и `Выполнение` с обратной полярностью: у них присваивание Ложь — штатный приём, а переносится требование присваивать под условием. Источник: «Эти же требования справедливы для других аналогичных параметров обработчиков событий: СтандартнаяОбработка, Выполнение и др. Например: … Если Параметры.Свойства(...) Тогда СтандартнаяОбработка = Ложь;» (стандарт 686, пункт 2).

**`SKILL.md:139-141`** — из двух средств оставлены только параметры процедур, годные лишь в контексте одного вызова; реквизиты формы и предупреждение о серверных переменных потеряны. Источник: «Реквизиты формы – если требуется сохранять промежуточные результаты между разными вызовами с клиента. (Следует иметь в виду, что значения серверных переменных модуля формы не сохраняются между вызовами с клиента.)» (стандарт 639, пункт 3.2).

**`SKILL.md:145-147`** — заголовок отдаёт заполнение структуры параметров вызываемой стороне, тогда как заготовку создаёт функция-конструктор, а конкретные значения проставляет вызывающий код. Источник: «…структуру-заготовку со свойствами, или пустую таблицу значений (дерево значений) с типизированными колонками, которую вызывающий код должен проинициализировать конкретными значениями и передать в вызываемую функцию» (стандарт 641, пункт 1.1).

**`SKILL.md:36-39`** — раздел «Что здесь необратимо» приписывает явной записи движений молчаливую порчу данных, обнаруживаемую через недели; источник называет прямо противоположное — громкий немедленный отказ. Источник: «В случае же нарушения этого правила, при параллельной работе нескольких пользователей, возможна ситуация возникновения взаимных блокировок при проведении документов» (стандарт 450, пункт 1).

### important

**`SKILL.md:52-54`** — взят только запрет на клиенте, предписывающая половина пункта потеряна, и агент остаётся без санкционированного выхода. Источник: «Как правило, вместо вызова функции ТекущаяДата на клиенте необходимо передавать с сервера на клиент время и дату, приведенную к часовому поясу пользовательского сеанса; при работе с документами на клиенте, использовать дату документа» (стандарт 643, пункт 3.1).

**`SKILL.md:41-73`** — штатный ответ БСП на вопрос «как получить дату на клиенте» не приведён; `ОбщегоНазначенияКлиент.ДатаСеанса()` в наборе не встречается ни разу. Источник: «В остальных случаях при использовании Библиотеки стандартных подсистем рекомендуется использовать функцию ДатаСеанса общего модуля ОбщегоНазначенияКлиент» (стандарт 643, пункт 3.4).

**`SKILL.md:70-72`** — «следует избегать» превращено в «не вызывается», «многократного обращения» сужено до «дважды», вторая функция из скобок потеряна. Источник: «Следует избегать в коде одной процедуры (функции) многократного обращения к функции ТекущаяДатаСеанса (ТекущаяДата), так как возвращаемые значения будут отличаться друг от друга» (стандарт 643, пункт 5).

**`SKILL.md:66-68`** — обоснование «иначе цены и остатки берутся не на тот момент» дописано: источник дефектом называет сам вызов на клиенте, а модальность — «следует рассмотреть возможность», а не «лучше». Источник: «При работе с документами следует рассмотреть возможность использования даты самого документа вместо текущей даты» (стандарт 643, пункт 3.3).

**`SKILL.md:90`** — селективность подана как единственное условие индексирования: «при двух и более операциях поиска», «большое количество строк» и отрицательный эффект лишнего индекса потеряны. Источник: «При двух и более операциях поиска в объекте ТаблицаЗначений с большим количеством строк рекомендуется: Индексировать колонки, по которым выполняется поиск… В противном случае, индексирование не даст эффекта, либо он будет отрицательным» (стандарт 452, пункт 1).

**`SKILL.md:98-100`** — названа проблема без решения: единственный санкционированный приём в наборе не упоминается нигде. Источник: «В остальных случаях – сортировать «по ссылке», а не по представлению. Для этого в методе Сортировать следует использовать объект СравнениеЗначений» (стандарт 452, пункт 3).

**`SKILL.md:84-87`** — вводное условие отброшено (в пункте 2.1 оно сохранено — набор непоследователен), «следует обеспечить» подменено словом «требует». Источник: «При использовании метода НайтиСтроки в таблицах значений с большим количеством строк следует обеспечить, чтобы список полей индекса был точно таким же…» (стандарт 452, пункт 2.2).

**`SKILL.md:95-96`** — потеряна привязка к случаям многократного поиска по большому объёму, «следует заменять» сведено к констатации «заменяется». Источник: «В указанных выше случаях объект ДеревоЗначений следует заменять индексированным объектом ТаблицаЗначений» (стандарт 452, пункт 4).

**`SKILL.md:79-96`** — «большое количество строк» повторено четырежды без единого числа, и вместе с порогом потеряно правило считать кратность поиска. Источник: «следует ориентироваться на 1000 строк и более, а также учитывать не только размер таблицы, в которой выполняется поиск, но и сколько раз он выполняется… даже если таблица относительно небольшая в 100 строк, но поиск по ней выполняется 100 раз, ее тоже имеет смысл индексировать» (стандарт 452, примечание к пункту 1).

**`SKILL.md:122-123`** — опасность сведена к чужим подпискам, тогда как главная ветка — несколько последовательных проверок внутри одного обработчика; обе правильные формы не приведены. Источник: «…параметр Отказ может устанавливаться сразу в нескольких последовательных проверках (или в нескольких подписках на одно и то же событие)… Кроме того, при доработках конфигурации на внедрении число этих проверок может увеличиться» (стандарт 686, пункт 1).

**`SKILL.md:116-126`** — изложение обрывается на пункте 2; выпал пункт с самой сильной модальностью и предписанной формой `ОбщегоНазначения.СообщитьПользователю(…, Отказ)`. Источник: «Неприемлемо в событиях объекта ПриЗаписи, ПередЗаписью, ПередУдалением, ОбработкаПроведения, ОбработкаПроверкиЗаполнения и т.п устанавливать параметр Отказ в значение Истина без информирования пользователя о причинах» (стандарт 686, пункт 3).

**`SKILL.md:104-107`** — «как есть» в усечённом виде читается как запрет что-либо добавлять; разрешение уточнять текст, условие к нему и функция `УточнениеИсключения` потеряны. Источник: «Для понятности, текст можно уточнить описанием прикладной операции, а также дополнить пояснением возможных причин, если для категории ошибки в платформе не предусмотрено стандартное пояснение или его недостаточно» (стандарт 499, пункт 3.1).

**`SKILL.md:109-111`** — из трёх вариантов назван один, и потерян тот, который стандарт прямо отводит обработчикам записи. Источник: «вызывать исключение, указывая в методе ВызватьИсключение строковый код ошибки в третьем параметре Код. Этот вариант подходит… когда ошибка возникает в обработчиках событий ПередЗаписью, ПриЗаписи и т.п.» (стандарт 790, пункт 2.3).

**`SKILL.md:102-114`** — взят частный случай 3.1, а головное правило и единственное «Недопустимо» стандарта не изложены; пустой перехват `/CheckConfig` не ловит. Источник: «Недопустимо перехватывать любые исключения, бесследно для системного администратора: Попытка … Исключение // перехват любых исключений КонецПопытки; Как правило, подобная конструкция скрывает реальную проблему, которую впоследствии невозможно диагностировать» (стандарт 499, пункт 3.2).

**`SKILL.md:102-126`** — выбор между исключением и `Отказ` в обработчиках записи и проведения не задан, хотя оба раздела стоят рядом. Источник: «Неприемлемо в событиях ОбработкаПроверкиЗаполнения, ОбработкаПроведения, ПередЗаписью, ПриЗаписи, ПередУдалением и т.п. вызывать исключения для выдачи останавливающих предупреждений… Вместо этого следует устанавливать параметр Отказ в значение Истина и выводить сообщения пользователю» (стандарт 790, пункт 3).

**`SKILL.md:146-148`** — цитата обрывается перед союзом «а также», и правка состава колонок переданной таблицы значений в вызывающем коде проходит как разрешённая. Источник: «В вызывающем коде не следует инициализировать структуру параметров или добавлять в нее какие-либо другие свойства, а также изменять состав и тип колонок таблицы значений» (стандарт 641, пункт 2).

**`SKILL.md:145-149`** — механизм, без которого запрет пункта 2 неисполним, отсутствует: слов «функция-конструктор параметров» в файле нет ни разу, а из пункта 1.2 взята только вторая фраза. Источник: «…необходимо определить функцию-конструктор параметров возвращающую: структуру-заготовку со свойствами» (стандарт 641, пункт 1.1); «Имена свойств структуры соответствуют параметрам вызываемой функции» (пункт 1.2).

**`SKILL.md:130-132`** — оставлен голый запрет без предписанной замены. Источник: «Процедуры и функции, которые работают по-разному при вызове с клиента и с сервера, следует размещать в общих модулях с постфиксами Клиент и Сервер, а не КлиентСервер» (стандарт 439, пункт 2).

**`SKILL.md:130-135`** — прямое разрешение на ветвление по режиму клиентского приложения вырезано, раздел читается как сплошной запрет препроцессора. Источник: «В то же время, как и в обычных клиентских модулях, допустимо ветвление кода для учета специфики различных режимов работы клиентского приложения: веб-клиент, тонкий или толстый клиент (например, #Если ВебКлиент)» (стандарт 439, пункт 2).

**`SKILL.md:130,134`** — «не следует» обеих норм превращено в «не использовать» и «не разрывают» с полужирным выделением, вопреки обещанию строк 26–29. Источник: «Не следует использовать инструкции препроцессора в клиент-серверных общих модулях…» (стандарт 439, пункт 2); «Не следует разрывать инструкциями препроцессора и областями отдельные грамматические конструкции…» (пункт 3).

**`SKILL.md:17-20`** — девять названных стандартов объявлены «формально относящимися к соглашениям о коде», хотя пересечение с разделом нулевое; под ту же формулу подпадает std643 — флагман этого файла. Источник: раздел «Соглашения при написании кода» — 35 стандартов (std456, std455, std647, std453, std640, std641 … std782), все девять лежат в разделе «Реализация обработки данных» (оглавление системы стандартов).

**`SKILL.md:164-167`** — «оно вынесено и здесь не повторяется» — выносить некуда: каталога `references` у навыка нет, и ни один из восьми стандартов в наборе не встречается. Источник: std455 «Структура модуля», std456 «Тексты модулей», std647, std453, std454, std441, std444, std440 (оглавление, раздел «Соглашения при написании кода»).

**`SKILL.md:153-156`** — цитата закрыта перед фразой о цене нарушения, вместе с ней потеряно условие «при параллельной работе нескольких пользователей». Источник: «В случае же нарушения этого правила, при параллельной работе нескольких пользователей, возможна ситуация возникновения взаимных блокировок при проведении документов» (стандарт 450, пункт 1).

### minor

**`SKILL.md:56-57`** — второй пример подменён (у источника фоновое задание, а не изменение данных), потеряно и уточнение, от какого именно пояса отметка не зависит. Источник: «Например, для определения момента перезаполнения закешированных данных, для получения времени последнего выполнения фонового задания и т.п.» (стандарт 643, пункт 2.2).

**`SKILL.md:43-46`** — нормативное «должны быть рассчитаны» подано как посылка («он исходит из того, что»), цитата обрезана на слове «пользователей». Источник: «Конфигурации должны быть рассчитаны на работу в условиях, когда часовой пояс на серверном компьютере не совпадает с реальным часовым поясом пользователей информационной базы» (стандарт 643, пункт 1).

**`SKILL.md:41-73`** — единственный в стандарте образец переноса получения даты на сервер при открытии формы не приведён. Источник: «Правильно — перенести получение текущей даты на сервер: ТекДата = Неопределено; // нет вызова ТекущаяДата() на клиенте … Объект.ПериодРегистрации = НачалоМесяца(ТекущаяДатаСеанса());» (стандарт 643, пункт 3.2).

**`SKILL.md:79-82`** — кавычка закрыта перед определяющим оборотом, а внутри кавычек проставлено отсутствующее в источнике выделение. Источник: «…даже если проиндексированы все колонки, обладающие хорошей селективностью» (стандарт 452, пункт 2.1).

**`SKILL.md:87-88`** — параметр назван описательно вместо имени из синтаксиса, потерян и его тип, по составу которого сопоставляется индекс. Источник: «Аналогичное ограничение действует и для метода Скопировать таблицы значений при вызове с параметром ПараметрыОтбора (Структура)» (стандарт 452, пункт 2.3).

**`SKILL.md:93`** — отсылка «(пункты 3.1 и 4)» читается как весь материал о массиве, но пункт про уникальность не отражён. Источник: «При необходимости обеспечить уникальность элементов в большом массиве следует однократно в конце алгоритма вызвать функцию СвернутьМассив или процедуру ДополнитьМассив с параметром ТолькоУникальныеЗначения = Истина» (стандарт 452, пункт 3.2).

**`SKILL.md:104-105`** — сужение области применения перед пунктом 3 не отражено, и правило переносится на мобильное приложение шире, чем разрешает источник. Источник: «Область применения (уточнение): управляемое приложение, обычное приложение» (стандарт 499, строка перед пунктом 3).

**`SKILL.md:110`** — выпало слово «вызова», и отказ от генерации исключений читается как отказ от `Попытка-Исключение`; оба обоснования запрета числовых кодов опущены. Источник: «отказаться от вызова исключений и возвращать строковые коды ошибок (коды возврата). При этом недопустимо использовать числовые коды ошибок, т.к. код становится нечитаемым; строковые литералы кодов ошибок не локализуются» (стандарт 790, пункт 2.3).

**`SKILL.md:134-135`** — перечень усечён до «процедур», функции формально выведены из-под правила; потеряна и отсылка к EDT. Источник: «Не следует разрывать инструкциями препроцессора и областями отдельные грамматические конструкции, выражения, а также объявления и места вызова процедур и функций» (стандарт 439, пункт 3).

**`SKILL.md:128`** — заголовок обещает директивы компиляции, под ним только пункты про препроцессор; единственный пункт о директивах не изложен нигде в наборе. Источник: «Директивы компиляции: &НаКлиенте, &НаСервере, &НаСервереБезКонтекста следует применять только в коде модулей управляемых форм и в коде модулей команд. В остальных модулях рекомендуется применять инструкции препроцессору» (стандарт 439, пункт 1).

---

## `d:/github.com/haflingmax/1c-agent-skills/skills/1c-managed-forms/references/texts-and-style.md`

### critical

**`texts-and-style.md:«Четыре правила обращения», правило 4`** — потеряно условие, при котором вид вообще включается в имя, а «рекомендуется включать» превращено в «ставится»; правило требует суффикса у всех элементов стиля, чему противоречат и примеры источника, и собственная таблица файла. Источник: «Для нескольких элементов стиля, имеющих одинаковое название, но разный вид, рекомендуется включать вид (слова "Цвет", "Шрифт", "Рамка") в название… При этом вид элемента стиля (Цвет, Шрифт, Рамка) следует указывать после его названия» (стандарт 667, пункт 5).

### important

**`texts-and-style.md:«Четыре правила обращения»`** — правил в стандарте пять; наш пункт 4 склеен из пунктов 4 и 5, и отсылка «п. 4» указывает не туда. Источник: «4. Название для элемента стиля следует подбирать таким образом, чтобы в нем отразить назначение элемента стиля. 5. Для нескольких элементов стиля, имеющих одинаковое название, но разный вид, рекомендуется включать вид…» (стандарт 667, пункты 4 и 5).

**`texts-and-style.md:таблицы «Цвет» и «Шрифт», третья колонка`** — колонка переименована в «Где применяется», хотя её значения — заголовки других стандартов, а не описание области применения. Источник: заголовок третьей колонки обеих таблиц — «В каком стандарте используется» (стандарт 667, таблицы элементов стиля).

**`texts-and-style.md:(6.2) «Вопрос задают перед»`** — перечень обрезан перед четвёртым случаем, «рекомендуется выдавать» превращено в «задают». Источник: «Вопросы рекомендуется выдавать перед выполнением: действий, результаты которых отменить потом невозможно; потенциально опасных для данных пользователя действий; массовой обработки информационной базы; длительных процедур» (стандарт 585, пункт 6.2).

**`texts-and-style.md:«зарезервированные сочетания самой платформы»`** — обобщения «через все буквы» и «F1–F10» выдуманы: они запрещают законные Ctrl+D/J/K/Q, добавляют отсутствующий F5 и переносят F7/F8 из прикладного списка в платформенный. Источник: платформенный список — Ctrl + A, B, C, E, F, G, H, I, L, N, O, P, S, T, U, V, W, X, Y, Z и F1, F2, F3, F4, F6, F9, F10 (стандарт 430, платформенная таблица).

**`texts-and-style.md:«Расхождение не разрешено самими стандартами»`** — расхождение 50/35 знаков разрешено самими источниками: это редакции 8.2 и 8.3, для 8.3 действует 35, а «выбор за задачей» узаконит названия, которые обрежутся многоточием. Источник: «См. также»: std712 → «Панель разделов (8.2)», «Названия разделов (8.2)»; std568 → «Панель разделов (8.3)» (стандарты 568 и 712; число — 712, пункт 2.1).

**`texts-and-style.md:«F8 для команды "Подобрать"»`** — два взаимоисключающих предписания на одну клавишу поставлены рядом без единого слова о конфликте источников. Источник: «F8 — Выключить/включить активность проводок в журнале операций», «Использование зарезервированных сочетаний в иных целях запрещается» (стандарт 430) против «Для команды «Подобрать» используйте акселератор F8» (стандарт 591).

### minor

**`texts-and-style.md:«со строки Alt + Num- подпись начинает относиться к предыдущему сочетанию»`** — дефект выгрузки описан неточно: у `Alt + Num-` пустая ячейка действия, а последующие пары привязаны верно. Источник: «Space — Изменение флажка», «Alt + Space — Вызвать системное меню приложения или модального диалога», «Alt + Hyphen (-) — Вызвать системное меню окна…» (стандарт 430, платформенная таблица).

**`texts-and-style.md:«### Панель сообщений (раздел 2)»`** — название раздела заменено собственным, поиск по стандарту ничего не найдёт. Источник: заголовок раздела 2 — «Сообщение об ошибках в форме» (стандарт 585, раздел 2).

**`texts-and-style.md:правило 2 раздела «Элементы стиля»`** — запрет сведён к совпадению имён, тогда как источник запрещает дублировать цвет. Источник: «Например, для гиперссылок нужно использовать цвет, предусмотренный в платформе, а не создавать для него свой элемент стиля с точно таким же цветом» (стандарт 667, пункт 2).

**`texts-and-style.md:строка таблицы «ПросроченныеДанные»`** — из области применения выброшено «или критичных», и ячейка перестаёт узнаваться как заголовок стандарта 584. Источник: «Акцентирование внимания на просроченных или критичных состояниях» (стандарт 667, таблица; заголовок стандарта 584).

**`texts-and-style.md:«ставить её первой — только "если есть уверенность…"»`** — цитата обрывается перед второй половиной правила, и куда девать команду в противном случае, не сказано. Источник: «Если нет, то второй или в другом месте» (стандарт 591).

**`texts-and-style.md:строка таблицы «Ширина панели навигации | 234 pt»`** — в колонке «Модальность источника» стоит наша характеристика «значение платформы», 234 pt читается как жёсткая константа, связанное правило выпадает. Источник: «По возможности, названия команд должны помещаться в ПН с учетом ее стандартной ширины ПН (234 pt)»; «…ширина ПН может быть уменьшена пользователем» (стандарт 573).

**`texts-and-style.md:«Стандарт ограничивает сам себя инструментом» и «Контур (2)»`** — «рассчитано на использование с» усилено до самоограничения, а в цитате про контур выпал его источник — модификатор. Источник: «Данное руководство рассчитано на использование с продуктами Adobe Photoshop и Axialis IconWorkshop»; «2. Контур. Для придания объектам читаемости, от модификатора идет внешний дополнительный контур на объект, шириной 1 пиксель» (стандарт 423, пункт 2).

**`texts-and-style.md:заголовки пунктов 4.1 и 5.1`** — в заголовках «рекомендуется» заменено утверждением («надо трижды», «только чтобы»), потеряна и оговорка «если есть возможность» у среднего из трёх выводов состояния. Источник: «Рекомендуется использовать состояние для информирования о выполнении длительных процессов… Выводите состояние: перед началом; в процессе выполнения (если есть возможность); при завершении»; «Предупреждение рекомендуется использовать только в тех случаях, когда…» (стандарт 585, пункты 4.1 и 5.1).

---

## `d:/github.com/haflingmax/1c-agent-skills/skills/1c-managed-forms/references/forms-and-fields.md`

### critical

**`forms-and-fields.md:39-41`** — пример о порядке заполнения повышен до правила о колонках и отправляет «Договор» в правую колонку, тогда как строка 98 того же файла кладёт его в левую; пункт источника с обратным примером не приведён. Источник: «Левая колонка … Например: - Номер и дата - Контрагент - Договор» (стандарт 722, пункт 1.4); «Левая колонка: Номер, дата / Вид операции / Контрагент / Банковский счет (контрагента) / Договор / Контактное лицо» (стандарт 597, пункт 2.2).

### important

**`forms-and-fields.md:14-41`** — заголовок объявляет раздел 1 стандарта 722, изложены только пункты 1.1–1.3; потеряны единственный прямой запрет раздела с пометкой «Обязательно к соблюдению» и числа табличной части. Источник: «Использование вертикальной зеленой черты для выделенных отдельных элементов и групп взаимосвязанных реквизитов не допускается» (стандарт 722, пункт 1.3.1); «Табличная часть документа должна содержать не менее 7-ми строк (оптимально — не менее 10-ти строк)» (пункт 1.4.1).

**`forms-and-fields.md:290`** — слово «целиком» — прямое утверждение о полноте, и оно неверно: приведено девять пунктов из одиннадцати. Источник: «В списках на рабочем столе по умолчанию должно быть видно не менее 3,5 строк» (стандарт 578, раздел «Что нужно учитывать при разработке»).

**`forms-and-fields.md:3-4`** — утверждение «она не одинакова» опровергается построчно, а обещание указать область применения у каждого стандарта не выполнено ни разу. Источник: «Область применения: управляемое приложение» — первая строка всех двенадцати цитируемых стандартов (722, 621, 597, 587, 588, 595, 716, 592, 607, 578, 582, 593).

**`forms-and-fields.md:194-198`** — из одиннадцати правил стандарта оставлены два о размещении; тексты гиперссылки не встречаются нигде в наборе. Источник: «Перед ссылкой выводится подпись "Счет-фактура"»; «Если счет-фактура не указан, рекомендуется выводить текст "Создать счет-фактуру"»; «Вместо гиперссылки выводится надпись "Недостаточно прав для создания счета-фактуры" в угловых скобках» (стандарт 595).

### minor

**`forms-and-fields.md:316-317`** — кавычка открыта после «рекомендуется», вводящий глагол заменён на «делают»; уточняющие пункты и оговорка о разнородности команд обрезаны. Источник: «Панель действий на рабочем столе рекомендуется делать только в том случае, если можно достоверно выделить частотные и не контекстные команды» (стандарт 578).

**`forms-and-fields.md:18`** — подводка молча переименовывает предмет: у источника «многостраничных», в файле «многострочных» (такого сочетания в базе v8std нет вовсе). Источник: «Заголовки многостраничных полей рекомендуется располагать в зависимости от общей композиции формы» (стандарт 722, пункт 1.1).

**`forms-and-fields.md:45-46`** — цитата обрезана перед хвостом, называющим средство, и читается как разрешение группировать по любому эстетическому соображению. Источник: «Для облегчения визуального восприятия формы за счет объединения элементов формы» (стандарт 621, раздел «Применение»).

**`forms-and-fields.md:120-123`** — отрицательное утверждение неверно: чередование ячеек в выгрузке восстанавливается однозначно, и сам файл им воспользовался, верно разложив девять ячеек. Источник: «Номер, дата + Вид операции» / «Организация + Банковский счет (организации)» / «Контрагент + Договор» / «Организация + Подразделение + Склад» / … (стандарт 597, раздел 3).

**`forms-and-fields.md:105-106`** — «все левые группы вокруг контрагента» неверно: их четыре, и первая контрагента не содержит. Источник: левая колонка — «Номер, дата + Вид операции», «Контрагент + Договор», «Контрагент + Банковский счет (контрагента)», «Контрагент + Контактное лицо» (стандарт 597, раздел 3).

**`forms-and-fields.md:285`** — оговорка названа «той же», хотя формулировка другая и работает через отсутствие полосы прокрутки. Источник: «Состав разделов рекомендуется проектировать так, чтобы в типовых комбинациях ролей не появлялась полоса прокрутки» (стандарт 567).

**`forms-and-fields.md:61-65`** — «разделён надвое» при трёх разделах, а заголовок «Шапка документа» сужает пункт, относящийся к любым формам. Источник: разделы «1. Общие рекомендации», «2. Оформление шапки, состоящей из двух колонок», «3. Примеры оформления конкретных групп полей»; «Один и тот же реквизит в разных формах рекомендуется размещать в одинаковом месте» (стандарт 597, пункт 1.1).

**`forms-and-fields.md:64-65`** — вывод сделан за источник: из заголовка раздела не следует, что при трёх колонках пункты 2.3 и 2.4 отменяются. Источник: «При наличии в форме нескольких взаимозависимых полей, их следует располагать последовательно – одно под другим» — без привязки к числу колонок (стандарт 597, пункт 2.3).

---

## `d:/github.com/haflingmax/1c-agent-skills/skills/1c-managed-forms/references/lists-and-tables.md`

### critical

**`lists-and-tables.md:34-35,64-66`** — правило «где таблица расходится с формулой, берётся таблица» и слово «норматив» выдуманы: таблица задаёт нижние границы, конфликта с формулой нет, и запрета применять формулу к её колонкам источник не вводит. Источник: «Если нужно понять какой ширины должна быть колонка или поле и известно типовое количество символов в значении, используйте формулу: Ширина колонки = Количество символов/1,25»; заголовок таблицы — «Минимальные размеры колонок для таблиц управляемой формы» (стандарт 618).

### important

**`lists-and-tables.md:64-65`** — перечень расхождений неверен: «Договор» и «Валюта» ведут себя как прочие округления, а настоящие отклонения — «Дата (со временем)» (15,20 против 13) и «Номер ГТД» (24,00 против 23) — не названы. Источник: «Дата (со временем) | 19 | 13», «Номер ГТД | 30 | 23», «Договор | 24 | 19», «Валюта | 3 | 3» (стандарт 618, таблица минимальных размеров).

### minor

**`lists-and-tables.md:120`** — отрицательное утверждение «число 10 — единственное в этом стандарте» опровергается самим файлом двадцатью строками выше. Источник: «элемент стиля вида шрифт "ПодсказкаНезаполненнойЯчейки" (размер: 8, начертание: наклонный)» (стандарт 717).

**`lists-and-tables.md:169-170`** — цитата оборвана перед второй половиной пункта, и указание, куда девать неитоговые элементы, потеряно. Источник: «Иные элементы следует располагать вне области итогов» (стандарт 718, пункт 1.2).

**`lists-and-tables.md:85`** — в коротком пересказе снята модальность: «рекомендуется размещать» стало безусловным, а «не менее 10-ти» — точечным «10 — оптимум». Источник: «Табличная часть документа должна содержать не менее 7-ми строк (оптимально — не менее 10-ти строк). Если количество строк в табличной части менее 7-ми — рекомендуется размещать табличную часть на отдельной вкладке» (стандарт 722, пункт 1.4.1).

**`lists-and-tables.md:7-9`** — половина оговорки описывает обозначение «абзац N», которого в файле нет ни разу. Источник: все ссылки файла даны как «пункт N» — «пункт 1.4.1» (стандарт 722), «пункт 1»–«пункт 4» (стандарт 717), «пункт 2.1»–«пункт 2.3» (стандарт 593).

---

## `d:/github.com/haflingmax/1c-agent-skills/skills/1c-managed-forms/references/command-interface.md`

### important

**`command-interface.md:21-23`** — требование к длине названия приписано стандарту 567, где его нет вовсе; верный адрес — стандарт 568, как и указано в соседнем файле того же справочника. Источник: «Общая длина названия не должна превышать примерно 50 знаков с учетом пробелов (примерно 150 точек при 96 DPI)» (стандарт 568).

**`command-interface.md:44`** — «не рекомендуется» усилено до «быть не должно» в сканируемом заголовке, чем блокируется законное решение. Источник: «Не рекомендуется делать раздел с названием «Сервис»» (стандарт 567; то же — стандарт 712, пункт 2.5).

**`command-interface.md:44-46`** — номер пункта взят у 712, текст — у 567, и всё выдано как одна общая цитата; внутри кавычек склеены два предложения источника. Источник: «…перекликаться с пунктом "Сервис" главного меню и группой "Сервис" в панели / меню функций» (стандарт 712, пункт 2.5) против «Не рекомендуется делать раздел с названием «Сервис». Т.к. он будет перекликаться с меню «Сервис» и группой «Сервис» в ПД» (стандарт 567, нумерации пунктов нет).

**`command-interface.md:39-41`** — цитата оборвана на двоеточии, за которым источник и определяет, что такое приоритет и в какую сторону строится порядок. Источник: «Рекомендуется определять порядок в зависимости от приоритета каждого раздела: от наиболее частотного и значимого до второстепенного и не часто используемого» (стандарт 567).

### minor

**`command-interface.md:46-48`** — открытый перечень подан закрытым списком из пяти названий, вопреки методу самого файла двумя абзацами ниже. Источник: «…«Сервисные функции», «Служебные функции» и т.д.» (стандарт 567).

**`command-interface.md:156`** — оговорка «нумерация по абзацам» не выполняется: пункт 5 склеивает четыре абзаца источника, пункт 6 — два, всего абзацев одиннадцать против восьми. Источник: «В такой форме размещаются все команды той или иной подсистемы»; «Большой размер рабочей области позволяет разместить также команды смежных областей…» (стандарт 401, методика).

**`command-interface.md:225-231`** — два самостоятельных положения пункта, разделённые в источнике блоками «Правильно/Неправильно», склеены в одну цитату без многоточия, и вывод «две модальности в одной фразе» фактически неверен. Источник: «Не рекомендуется делать группы, содержащие всего одну команду. Правильно Неправильно Запрещается делать группы, в которых выводится только одна "важная" команда» (стандарт 714, пункт 3.3).

**`command-interface.md:60-66`** — приведённая причина подана как единственная, главное обоснование пропущено, и правило выглядит производительностным, а не интерфейсным. Источник: «Лучше пользователю давать единообразные решения, так как иначе получается, что часть связанной информации он получает через панель навигации, а часть через переключение закладок» (стандарт 611).

**`command-interface.md:147-152`** — модальности в правиле нет ни одной, и не назван вход в правило. Источник: «Если отчетов много или если для управления ими пользователю нужен особый интерфейс, следует разработать специальное рабочее место… Команду вызова рабочего места рекомендуется поместить: в панель навигации – если рабочее место должно открываться в главном окне; в группу «Сервис» панели действий – если рабочее место должно открываться в отдельном окне» (стандарт 576).

**`command-interface.md:92-94`** — цитата обрывается перед расшифровкой термина, которую источник даёт в той же фразе. Источник: «…группа станет «вырожденной», т.е. состоящей всего из одной команды» (стандарт 574).

**`command-interface.md:245-246`** — цитата обрывается перед противовесом, и правило подталкивает к искусственным префиксам ради различимости. Источник: «…команды должны отличаться друг от друга уже в первых символах, но в тоже время должны быть понятны» (стандарт 573).

---

## Что проверено

Каждый проверяющий открывал первоисточник целиком (`tools/its-search.py --документ`, база v8std) и сверял построчно; каждая закавыченная строка файла прогонялась поиском по тексту источника. По каждому файлу сверялись: принадлежность номеров пунктов, область применения, модальность каждого правила, составные условия через «и»/«или», хвосты цитат, наличие обоснований в источнике, восстановление таблиц, развёрнутых выгрузкой ИТС в один столбец, и отрицательные утверждения файла.

**Соглашения при написании кода и обработка данных (SKILL.md):** 439, 450, 452, 499, 639, 641, 643, 686, 790 — целиком; 436, 437, 460, 648, 655, 656, 657, 729, 783 — целиком, для проверки утверждения о маршрутизации в `1c-queries`.

**Командный интерфейс:** 401, 567, 568, 572, 573, 574, 576, 606, 611, 623, 712, 714.

**Списки и таблицы:** 495, 578, 593, 604, 618, 653, 717, 718, 722.

**Формы и поля:** 503, 582, 587, 588, 592, 595, 597, 607, 621, 716 (плюс 567, 578, 593, 722 повторно).

**Тексты и оформление:** 423, 430, 468, 569, 584, 585, 591, 667 (плюс 568, 573, 576, 593, 712, 717, 718 повторно).

Всего 57 стандартов. Дополнительно: разделы оглавления системы стандартов («Соглашения при написании кода» — 35 стандартов, «Реализация обработки данных» — 33, «Создание и изменение объектов метаданных» — 50, всего 13 разделов верхнего уровня); фразовые поиски по базе v8std для подтверждения принадлежности спорных цитат; программная сверка таблицы ширин std618 (24 строки, пересчёт всех строк по формуле), обеих таблиц std667 и обеих таблиц std430 (425 строк выгрузки); `grep` по `skills/` на имена функций, номера стандартов и тексты правил — чтобы отличить потерю правила от переноса в соседний файл; подтверждено, что у навыка `1c-code-conventions` каталога `references` нет.
